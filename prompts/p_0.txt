# ══════════════════════════════════════════════════════════════════════
# STAGE 0 — Voice Casting
# ══════════════════════════════════════════════════════════════════════
#
# 📥 PLACEHOLDERS (replaced by run.sh via sed before calling claude -p):
#     {{PROJECT_SLUG}}  →  e.g. "the-pharaoh-who-defied-death"
#     {{EPISODE_ID}}    →  e.g. "s01e02"
#     {{EPISODE_DIR}}   →  e.g. "projects/the-pharaoh-who-defied-death/episodes/s01e02"
#     {{LOCALES}}       →  e.g. "en, zh-Hans"
#     {{STORY_FORMAT}}  →  e.g. "episodic"
#
# 📥 FILES READ:
#     {{EPISODE_DIR}}/meta.json             (all pipeline variables — source of truth)
#     {{EPISODE_DIR}}/story.txt             (story content, used for voice casting only)
#     {{EPISODE_DIR}}/Script.json           (if present — cast[] overrides story.txt for Script.json format)
#     prompts/azure_tts_styles.txt          (inlined by run.sh)
#
# 📤 OUTPUT (stdout printed to web UI + files written to disk):
#     • Stdout: variable confirmation block  (web UI sees this)
#     • projects/{{PROJECT_SLUG}}/VoiceCast.json   ← voice casting (append-only across episodes)
#     • {{EPISODE_DIR}}/pipeline_vars.sh           ← overwritten with full content
#
# ▶ Invoked by run.sh as:
#     claude -p prompts/p_0_filled.txt
# ══════════════════════════════════════════════════════════════════════

You are Stage 0 of a 10-stage AI narrative pipeline.
Your ONLY jobs: read meta.json, cast voices, write VoiceCast.json,
and overwrite pipeline_vars.sh with full content.
Do NOT generate any story content.
Do NOT re-derive, change, or "correct" any variable values from meta.json.

---

## Task

Print: `▶ [0/9] Stage 0 — Voice casting …`

---

### Step 1 — Read inputs

Read these files:
  1. `{{EPISODE_DIR}}/meta.json`        — all pipeline variables are here; use them exactly as-is
  2. `{{EPISODE_DIR}}/story.txt`        — story content (may be placeholder for Script.json format)
  3. `{{EPISODE_DIR}}/Script.json`      — if present, cast[] overrides character discovery from story.txt
  4. `prompts/azure_tts_styles.txt`     — available Azure voices and styles

Extract these variables from meta.json:

| Variable          | meta.json field       |
|-------------------|-----------------------|
| STORY_TITLE       | story_title           |
| EPISODE_ID        | episode_id            |
| LOCALES           | locales               |
| PROJECT_SLUG      | project_slug          |
| SERIES_GENRE      | series_genre          |
| STORY_FORMAT      | story_format          |
| EPISODE_NUMBER    | episode_number        |
| GENERATION_SEED   | generation_seed       |
| RENDER_PROFILE    | render_profile        |

**Do NOT re-derive, change, or "correct" any of these values.**

---

### Step 2 — Cast voices for each locale

Using the story file and prompts/azure_tts_styles.txt you already read:

#### Step 2a — Identify main characters

**Format-aware cast scope — apply BEFORE listing characters:**

| STORY_FORMAT            | Characters to cast                                                  |
|-------------------------|---------------------------------------------------------------------|
| `episodic`              | narrator + protagonist + antagonist + supporting (typically 2–5)   |
| `continuous_narration`  | **narrator ONLY** — no protagonist, no other characters            |
| `illustrated_narration` | **narrator ONLY** — characters appear visually but never speak     |
| `documentary`           | **narrator ONLY** — no characters, narrator explains everything    |
| `monologue`             | **protagonist ONLY** — protagonist IS the narrator; do NOT create a separate narrator entry |
| `Script.json`           | All characters listed in Script.json cast[] — use character_id and gender exactly as given; do NOT infer additional characters from story.txt |

For `episodic`: narrator is mandatory. Every episode has off-screen narration lines.
Narrator is not a story character but must appear in VoiceCast.json with its own voice.

For `monologue`: the protagonist speaks directly to the audience and fills the narrator
role. Do NOT add a "narrator" entry — Stage 3 will use the protagonist's character_id
for all lines.

#### Step 2b — For each character × each locale in LOCALES

  a. Note gender and personality traits (commanding, fearful, ancient, young, etc.).

  b. From azure_tts_styles.txt, pick the BEST matching Azure voice:
       - Narrator             → prefer voices with "narration-professional"
                                or "newscast" style
       - Emotional lead       → prefer voices with wide style coverage
                                (en: AriaNeural; zh: XiaoxiaoNeural)
       - Villain/antagonist   → prefer voices with angry/disgruntled/serious
       - zh-CN leads          → prefer DragonHDFlashLatest voices (richer range)
       - Match gender strictly (male/female/neutral)
       - Every character MUST get a DIFFERENT azure_voice per locale.
         Narrator and protagonist especially must never share a voice.
         If fewer distinct voices exist than characters, prioritise:
         protagonist → antagonist → narrator → minor characters (minor may share).

  b2. **Supports() check:**
       When selecting a voice for a locale, verify its `supports()` list includes
       that locale.  Multilingual voices may list both en-* and zh-* in supports()
       and may be assigned to either locale.

  b3. **Emotional role casting rule:**
       Do NOT assign a voice with `styles(0): []` to any character who requires
       emotional expression: antagonist, emotional lead, anger, fear, grief,
       warmth, suspense.  No-style voices produce flat, affectless output.
       No-style voices are only suitable for:
         – narrator in documentary / illustrated_narration / continuous_narration
         – minor background characters with neutral delivery

  b4. **Multitalker restriction:**
       NEVER assign a MULTITALKER voice as a character voice.
       Voices in the [MULTITALKER] section are Azure pair-synthesis voices; they
       require two named speakers inside one SSML call and cannot be used as a
       single character voice.  Assigning one produces broken or silent TTS output.
       Treat every [MULTITALKER] entry as reserved — skip them during casting.

  c. List the styles that voice actually supports (copy from azure_tts_styles.txt).

  d. Set base azure_pitch and azure_break_ms for the character archetype:
       - Narrator:    pitch "-5%",  break_ms 600
       - Elder/Wise:  pitch "-10%", break_ms 800
       - Young hero:  pitch "0%",   break_ms 400
       - Villain:     pitch "-5%",  break_ms 500
       - Young child: pitch "+5%",  break_ms 350

---

### Step 3 — Write VoiceCast.json

Path: `projects/{{PROJECT_SLUG}}/VoiceCast.json`

If this file already exists, READ it first.  Only ADD entries whose
character_id is not already present — NEVER overwrite existing entries
(preserves voice consistency across episodes of the same series).

Format:
```json
{
  "schema_id": "VoiceCast",
  "schema_version": "1.0.0",
  "project_id": "<PROJECT_SLUG>",
  "characters": [
    {
      "character_id":   "narrator",
      "role":           "narrator",
      "gender":         "neutral",
      "personality":    "authoritative, cinematic",
      "en": {
        "azure_voice":        "en-US-GuyNeural",
        "available_styles":   ["newscast", "angry", "cheerful", ...],
        "azure_pitch":        "-5%",
        "azure_break_ms":     600,
        "azure_style_degree": 1.3
      },
      "zh-Hans": {
        "azure_voice":        "zh-CN-YunxiNeural",
        "available_styles":   ["angry", "cheerful", "sad", ...],
        "azure_pitch":        "-5%",
        "azure_break_ms":     600,
        "azure_style_degree": 1.3
      }
    }
  ]
}
```

Rules:
  • character_id must be a lowercase slug (e.g. "narrator", "pharaoh", "anubis").
    Stage 3 MUST use these exact IDs as cast[].character_id in Script.json.
  • Include ONLY locales present in LOCALES (from meta.json).
  • available_styles: copy exactly from azure_tts_styles.txt for the chosen voice.

---

### Step 4 — Overwrite pipeline_vars.sh

Path: `{{EPISODE_DIR}}/pipeline_vars.sh`

Overwrite the file completely. Fill in actual values read from meta.json:

```bash
export STORY_TITLE="<from meta.json: story_title>"
export EPISODE_NUMBER="<from meta.json: episode_number>"
export EPISODE_ID="<from meta.json: episode_id>"
export LOCALES="<from meta.json: locales>"
export PROJECT_SLUG="<from meta.json: project_slug>"
export SERIES_GENRE="<from meta.json: series_genre>"
export GENERATION_SEED="<from meta.json: generation_seed>"
export RENDER_PROFILE="<from meta.json: render_profile>"
export STORY_FORMAT="<from meta.json: story_format>"
export PROJECT_DIR="projects/<PROJECT_SLUG>"
export EPISODE_DIR="projects/<PROJECT_SLUG>/episodes/<EPISODE_ID>"
export VOICE_CAST_FILE="projects/<PROJECT_SLUG>/VoiceCast.json"
```

After writing, print this exact confirmation block to stdout:

```
── Stage 0 complete ────────────────────────────────────────────────────
STORY_TITLE      : <value>
EPISODE_NUMBER   : <value>
EPISODE_ID       : <value>
LOCALES          : <value>
PROJECT_SLUG     : <value>
SERIES_GENRE     : <value>
GENERATION_SEED  : <value>
RENDER_PROFILE   : <value>
STORY_FORMAT     : <value>
VOICE_CAST_FILE  : <value>
────────────────────────────────────────────────────────────────────────
```

Print: `✓ [0/9] Stage 0 — Voice casting done`
