# Regression Verification Guideline — AI Narrative Pipeline
# ══════════════════════════════════════════════════════════════════════════════
#
# PURPOSE: Before or after any modification to this codebase, verify all
# workflows described here still function correctly end-to-end.  Each section
# lists the exact files involved, the invariants that must hold, and the
# specific things most likely to break when that area is touched.
#
# CODEBASE ROOT: (repo root — directory containing run.sh)
# ══════════════════════════════════════════════════════════════════════════════

---

## PART 1 — SYSTEM ARCHITECTURE

The pipeline converts a plain-text story into a multi-locale rendered video episode.
It has four layers:

```
┌─────────────────────────────────────────────────────────────┐
│  Web UI  (test_server.py — single-file Python HTTP server)  │
│  • Runs on localhost:8080 (or 192.168.86.40:8080)           │
│  • Serves HTML+JS UI as a large inline string               │
│  • Orchestrates all workflows via HTTP endpoints            │
│  • Media tab proxies requests to Media Search Server        │
│  • Env: MEDIA_SERVER_URL, MEDIA_API_KEY                     │
└────────────┬─────────────────────────────┬──────────────────┘
             │  spawns subprocess          │  HTTP proxy
┌────────────▼──────────────────┐  ┌───────▼──────────────────────┐
│  run.sh  (pipeline orch.)     │  │  Media Search Server         │
│  • Stages 0–8: claude -p      │  │  (code/media/http/server.py) │
│  • Stage 9: Python scripts    │  │  • FastAPI on port 8200      │
│  • Hard stop after Stage 5    │  │  • NFS server 192.168.86.33  │
│  •   (media selection gate)   │  │  • Pexels/Pixabay download   │
│  • Hard stop after Stage 8    │  │  • CLIP scoring + ranking    │
│  •   (pre-render checkpoint)  │  │  • Env: MEDIA_API_KEY,       │
│  • Streams stdout via SSE     │  │    PEXELS_API_KEY,           │
└────────────┬──────────────────┘  │    PIXABAY_API_KEY           │
             │  writes/reads       └───────┬──────────────────────┘
┌────────────▼─────────────────────────────▼──────────────────┐
│  projects/{PROJECT_SLUG}/                                   │
│  ├── VoiceCast.json            (Stage 0, shared across eps) │
│  ├── canon.json                (Stage 7, grows per ep)      │
│  └── episodes/{EPISODE_ID}/                                 │
│      ├── story.txt             (input, written by UI)       │
│      ├── meta.json             (written by Create Episode)  │
│      ├── pipeline_vars.sh      (stub→full, Stage 0)         │
│      ├── StoryPrompt.json      (Stage 2)                    │
│      ├── Script.json           (Stage 3)                    │
│      ├── ShotList.json         (Stage 4)                    │
│      ├── AssetManifest_draft.shared.json  (Stage 5)         │
│      ├── AssetManifest_draft.{locale}.json (Stage 5 / 8)   │
│      ├── canon_diff.json       (Stage 6)                    │
│      ├── assets/media/selections.json  (VC editor Confirm)  │
│      ├── assets/media/{batch_id}/      (media server state) │
│      │   └── batch_state.json                                │
│      ├── AssetManifest_merged.{locale}.json  (Stage 9[2/8]) │
│      ├── AssetManifest.media.{locale}.json   (Stage 9[5/8]) │
│      ├── RenderPlan.{locale}.json            (Stage 9[6/8]) │
│      └── renders/{locale}/output.mp4         (Stage 9[7/8]) │
└─────────────────────────────────────────────────────────────┘
```

---

## PART 2 — WORKFLOW A: NEW EPISODE CREATION (Prepare → Run)

### Files involved
- `code/http/test_server.py` — JS functions: `runPrepare()`, `createEpisode()`, `applyPreparedMeta()`, `runPrompt()`
- `code/http/test_server.py` — API endpoints: `POST /api/infer_story_meta`, `GET /api/check_slug`, `GET /api/next_episode_id`, `POST /api/create_episode`

### Step-by-step flow

**Step 1 — User pastes story into textarea and clicks "⚙ Prepare"**
- JS: `runPrepare()` (search: `async function runPrepare`)
- Calls `POST /api/infer_story_meta` with `{story: <text>}`
- Server runs: `claude -p --model haiku` with a one-shot extraction prompt
- Returns: `{title, slug, genre, story_format, metadata_found[], slug_exists, slug_suggested}`
- JS stores result in `_preparedMeta`, sets `_selectedFormat`
- JS calls `applyPreparedMeta(d)` to populate the info bar fields

**Step 2 — applyPreparedMeta() branch on slug_exists**
- `slug_exists == false` (new project):
  - Calls `GET /api/next_episode_id?slug=<slug>`
  - Server returns `{"next_ep_id": "s01e01"}` (scans `projects/<slug>/episodes/` for highest sNNeNN)
  - JS: `_preparedEpId = nd.next_ep_id || 's01e01'`  ← KEY: reads `next_ep_id`, not `ep_id`
  - Displays the next episode ID in the info bar (🆔 Episode field)
  - **Enables the Run button immediately** — there is NO separate "Create Episode" button
- `slug_exists == true` (existing project):
  - JS: `_preparedEpId = null`
  - Calls `loadRunProjects().then(onRunProjectChange)` — auto-selects project in Run tab
  - Run button becomes enabled once `onRunProjectChange` → `loadEpisodeDetails` sets `currentSlug`/`currentEpId`

**Step 3 — User clicks "▶ Run"**
- JS: `runPrompt()` detects that `currentSlug`/`currentEpId` are not yet set (new project)
- Automatically calls `createEpisode()` before starting the SSE stream:
  - `createEpisode()` calls `POST /api/create_episode` with: `{slug, ep_id, story, title, genre, story_format, locales, no_music}`
  - Server creates:
    1. `projects/{slug}/episodes/{ep_id}/` directory
    2. `story.txt` — raw story content
    3. `meta.json` — full episode metadata (see schema below)
    4. `pipeline_vars.sh` — stub with all vars EXCEPT `VOICE_CAST_FILE` (Stage 0 adds that)
  - Server returns: `{ok: true, ep_dir: "projects/slug/episodes/ep_id", slug, ep_id}`
  - JS: sets `currentSlug = d.slug`, `currentEpId = d.ep_id`
- `runPrompt()` then proceeds to open the SSE stream with `ep_dir` built from the now-set `currentSlug`/`currentEpId`

**NOTE — No "Create Episode" button exists:**
`createEpisode()` is an internal async helper, not a user-facing button. It is called
automatically by `runPrompt()` the first time the user clicks Run for a new project.
Do NOT add a standalone "Create Episode" button — it breaks the workflow by requiring
an extra click and creates ambiguity about when the episode dir exists.

### meta.json schema (written by /api/create_episode)
```json
{
  "schema_id":       "EpisodeMeta",
  "story_title":     "...",
  "project_slug":    "...",
  "episode_id":      "s01e01",
  "episode_number":  "01",
  "series_genre":    "...",
  "story_format":    "episodic",
  "locales":         "en",
  "generation_seed": 123456789,
  "render_profile":  "preview_local",
  "no_music":        false,
  "created_at":      "2026-02-28T..."
}
```

### pipeline_vars.sh stub (written by /api/create_episode)
```bash
export STORY_TITLE="..."
export EPISODE_NUMBER="01"
export EPISODE_ID="s01e01"
export LOCALES="en"
export PROJECT_SLUG="..."
export SERIES_GENRE="..."
export GENERATION_SEED="123456789"
export RENDER_PROFILE="preview_local"
export STORY_FORMAT="episodic"
export PROJECT_DIR="projects/..."
export EPISODE_DIR="projects/.../episodes/s01e01"
# NOTE: VOICE_CAST_FILE is NOT in the stub — Stage 0 adds it
```

### Regression risks for Workflow A
1. `/api/next_episode_id` returns key `"next_ep_id"` — JS must read `nd.next_ep_id`, NOT `nd.ep_id`
2. Fallback episode ID must be `'s01e01'` (not `'ep0001'` or other formats)
3. `currentSlug` and `currentEpId` must be set by `createEpisode()` BEFORE the SSE stream opens (inside `runPrompt()`, synchronously awaited)
4. `createEpisode()` must POST to `/api/create_episode` and receive `{ok, slug, ep_id}`
5. `meta.json` field names must match what `p_0.txt` reads: `story_title`, `project_slug`, `episode_id`, `episode_number`, `series_genre`, `story_format`, `locales`, `generation_seed`, `render_profile`
6. `pipeline_vars.sh` stub must export all vars that `run.sh` uses via `{{PLACEHOLDER}}` substitution (see PART 3)
7. `runPrompt()` auto-create guard: must check `!currentSlug || !currentEpId` (both) before calling `createEpisode()` — avoids duplicate creation if Run is clicked again on an existing episode

---

## PART 3 — WORKFLOW B: PIPELINE EXECUTION (run.sh Stages 0–10)

### Entry point
File: `run.sh`
Invoked by: web UI `/stream` endpoint as `bash run.sh <ep_dir> <from_stage> <to_stage>`
Working directory: `PIPE_DIR` (repo root)

### How run.sh works

**Invocation signature:**
```bash
./run.sh projects/{slug}/episodes/{ep_id} [from_stage=0] [to_stage=10]
```

**On startup, run.sh:**
1. Validates `ep_dir` exists and `story.txt` + `pipeline_vars.sh` are present
2. Sources `pipeline_vars.sh` immediately (stub values available before Stage 0)
3. Runs stages `FROM_STAGE` through `TO_STAGE` inclusive

**fill_and_run(N) — called for each LLM stage:**
1. Reads `prompts/p_N.txt`
2. Runs `sed` substitution replacing all `{{PLACEHOLDER}}` tokens:
   - `{{EPISODE_DIR}}`    → `$EP_DIR`  (e.g. `projects/slug/episodes/s01e01`)
   - `{{STORY_FILE}}`     → `${EP_DIR}/story.txt`
   - `{{PROJECT_SLUG}}`   → from pipeline_vars.sh
   - `{{EPISODE_ID}}`     → from pipeline_vars.sh
   - `{{EPISODE_NUMBER}}` → from pipeline_vars.sh
   - `{{STORY_TITLE}}`    → from pipeline_vars.sh
   - `{{SERIES_GENRE}}`   → from pipeline_vars.sh
   - `{{GENERATION_SEED}}`→ from pipeline_vars.sh
   - `{{RENDER_PROFILE}}` → from pipeline_vars.sh (default: preview_local)
   - `{{LOCALES}}`        → from pipeline_vars.sh (default: en)
   - `{{STORY_FORMAT}}`   → from pipeline_vars.sh (default: episodic)
3. Calls `inline_files_into_prompt` — detects lines matching `  N. some/path.json` and pre-embeds the file content into the prompt (eliminates Read tool calls in Claude)
4. Calls `claude -p --model <model> --dangerously-skip-permissions --no-session-persistence --append-system-prompt "<exec_directive>" <filled_prompt>`
5. Tees output to `stage_logs/{slug}.{ep_id}.stage_N.log` and stdout

**Model selection (get_stage_model):**
- Stages 2, 3, 4, 5, 8 → `sonnet` (creative/complex)
- All others → `haiku` (extraction/assembly)
- Global override: `MODEL=opus ./run.sh` overrides all
- Per-stage override: `STAGE_MODEL_3=opus ./run.sh` overrides one stage

**After Stage 0 specifically:** run.sh re-sources `pipeline_vars.sh` to pick up `VOICE_CAST_FILE` and `EPISODE_DIR` exports that Stage 0 added.

### Hard stops (pipeline checkpoints)

**After Stage 5 — media selection gate:**
- Checks `${EP_DIR}/assets/media/selections.json`
- If missing AND `TO_STAGE > 5`: prints instructions and `exit 0`
- If present: prints `✓ Media selections found` and continues
- User resumes with: `./run.sh <ep_dir> 6`
- Purpose: Stage 9 `resolve_assets.py` needs `selections.json` to map stock media

**After Stage 8 — pre-render checkpoint:**
- Always fires when `TO_STAGE > 8` (regardless of selections.json)
- Shows checklist: media selections (✓ or ⚠), manifest, render plan
- `exit 0` — user verifies artefacts, then resumes: `./run.sh <ep_dir> 9`
- Purpose: prevent accidentally running a long Stage 9 render before review

**Both use `exit 0` (clean pause), not `exit 1` (error).**

**Normal new-project flow:**
```bash
./run.sh <ep_dir>           # runs 0→5, pauses (no selections.json)
# → UI Media tab → search → select → Confirm
./run.sh <ep_dir> 6         # runs 6→8, pauses (pre-render)
# → verify artefacts
./run.sh <ep_dir> 9         # runs Stage 9 only (render)
```

### Regression risks for run.sh
1. Every `{{PLACEHOLDER}}` used in any `prompts/p_N.txt` MUST have a matching `-e "s|{{...}}|...|g"` line in `fill_and_run`'s `sed` block. Adding a new placeholder to any prompt REQUIRES adding the corresponding sed rule.
2. The `inline_files_into_prompt` function detects file paths using regex `^[[:space:]]+[0-9]+\.[[:space:]]+([^[:space:]]+\.(json|txt|sh))[[:space:]]*`. Only `.json`, `.txt`, `.sh` are inlined. If a prompt references other file types with the numbered-list format, they won't be inlined.
3. `set -euo pipefail` means any stage failure exits the whole script immediately.
4. Stage 0 MUST write `pipeline_vars.sh` — run.sh checks for its existence after Stage 0 and exits with error if missing.
5. Hard stop after Stage 5 checks `${EP_DIR}/assets/media/selections.json` — exits 0 if missing, continues if present
6. Hard stop after Stage 8 always exits 0 when `TO_STAGE > 8` — unconditional pre-render pause
7. `resolve_assets.py` call passes `--selections <path>` when `selections.json` exists on disk
8. `NO_MUSIC=1` env var: Stage 9 passes `--no-music` to `render_video.py` only if this is set. The web UI `/stream` handler reads `no_music` from `meta.json` if not overridden by checkbox.

---

## PART 4 — PIPELINE STAGES 0–9 (LLM-driven)

### Stage 0 — Voice Casting
**File:** `prompts/p_0.txt` (LLM path — episodic/monologue only)
**Deterministic alternative:** `gen_pipeline_vars.py` + `voice_cast_narrator.py` for `continuous_narration`, `illustrated_narration`, `documentary`
**Model:** haiku (episodic/monologue), none (narration formats)
**Reads:**
- `{{EPISODE_DIR}}/meta.json` — all pipeline variables (source of truth)
- `{{EPISODE_DIR}}/story.txt` — for voice casting decisions only
- `prompts/azure_tts_styles.txt` — styled voices only (62 entries); pre-embedded by run.sh

**Writes:**
- `projects/{{PROJECT_SLUG}}/VoiceCast.json` — append-only per character_id (preserves voices across episodes)
- `{{EPISODE_DIR}}/pipeline_vars.sh` — FULL version replacing stub; adds `VOICE_CAST_FILE` and `EPISODE_DIR` exports

**Critical invariants:**
- MUST NOT change/correct any values from meta.json — copies exactly
- VoiceCast.json is APPEND-ONLY: only adds characters not already present
- Every character gets a DIFFERENT azure_voice per locale
- For `monologue` format: NO narrator entry — protagonist IS the narrator
- For `continuous_narration`, `illustrated_narration`, `documentary`: narrator ONLY, no other characters
- pipeline_vars.sh MUST include all vars used by run.sh for stages 1–9
- Do NOT cast a `styles(0): []` (no-style) voice for any character requiring emotional expression (antagonist, emotional lead, anger, fear, grief). No-style voices are only suitable for narrator roles in documentary/narration formats or minor neutral-delivery characters
- NEVER cast a `[MULTITALKER]` voice as a character voice — these are Azure pair-synthesis voices, not usable as individual character voices; they produce broken TTS output
- Before selecting a voice for a locale, verify its `supports()` list includes that locale. Multilingual voices may list both en-* and zh-* in `supports()` and may be assigned to either locale. Never assign a voice to a locale not in its `supports()` list.

**Narrator format deterministic path (`gen_pipeline_vars.py` + `voice_cast_narrator.py`):**
- `gen_pipeline_vars.py` reads `meta.json`, writes full `pipeline_vars.sh` (all 13 vars, same format as LLM would produce)
- `voice_cast_narrator.py` parses `prompts/azure_tts_styles.txt`, selects best narrator voice per locale (prefers `narration-professional` style), writes/appends to `VoiceCast.json`
- Used for `continuous_narration`, `illustrated_narration`, `documentary` — formats that only need a narrator character

**ssml_narration alternative:** When `STORY_FORMAT=ssml_narration`, Stage 0 runs `ssml_preprocess.py` instead of `fill_and_run 0`. This script:
- Parses authored SSML from `story.txt` to extract voice params, sentence structure, and inner content
- Writes: `Script.json`, `VoiceCast.json` (append-only), `ssml_inner.xml`, `pipeline_vars.sh` (full), `NarrationText.txt`
- Replaces stages 0, 1, 2, 3 in one shot (those stages are skipped via run.sh guards)

**What breaks if Stage 0 fails or writes wrong values:**
- All downstream stages source `pipeline_vars.sh` — wrong values propagate everywhere
- VoiceCast character_ids must match what Stage 3 uses as `cast[].character_id`

---

### Stage 1 — Canon Check
**File:** `prompts/p_1.txt` (LLM path — no longer used)
**Deterministic replacement:** `canon_check.py` (Python, all formats)
**Model:** none (deterministic)
**Reads:** `projects/{{PROJECT_SLUG}}/canon.json` (may not exist for ep 1)
**Writes:** stdout report (captured to `stage_logs/{slug}.{ep_id}.stage_1.log` via `tee`)

**Critical invariants:**
- If canon.json doesn't exist: NOT an error; episode is treated as episode 1
- Output is informational only; no downstream stage reads `stage_1_check.txt`
- `canon_check.py` prints a structured report (character count, world facts, threads, episode summaries) — always > 100 bytes for log-based done-detection

**Pipeline tab done detection (important):**
Stage 1 done status in the Pipeline tab is determined by the **stage log file**, NOT by
`stage_1_check.txt`. This is because Claude often only prints the canon report to stdout
without writing the file. The log file is always written by `run.sh` via `| tee "$log_file"`
regardless of Claude's behavior.

Done signal: `stage_logs/{slug}.{ep_id}.stage_1.log`  exists AND  size > 100 bytes
Implemented in: `_pipeline_status()` in `test_server.py`

Do NOT change Stage 1 done detection back to checking `stage_1_check.txt` — that was the
root cause of Stage 1 never showing ✓ in the Pipeline tab despite completing successfully
in the Run tab.

---

### Stage 2 — Produce StoryPrompt.json
**File:** `prompts/p_2.txt`
**Model:** sonnet
**Reads:** `episode_direction.txt` (legacy path), `projects/{{PROJECT_SLUG}}/canon.json` (optional), `contracts/schemas/StoryPrompt.v1.json`
**Writes:** `{{EPISODE_DIR}}/StoryPrompt.json`

**Validates against:** `contracts/schemas/StoryPrompt.v1.json`

**Required fields:** `schema_id`, `schema_version`, `prompt_id`, `episode_goal`, `generation_seed`, `series.title`, `series.genre`, `series.tone`, `setting.primary_location`, `characters[]` (min 2), `constraints.max_scenes`

**Regression risks:**
- `characters[]` must have ≥ 2 entries or schema validation fails
- `generation_seed` must be an integer (not string)

---

### Stage 3 — Produce Script.json
**File:** `prompts/p_3.txt` (LLM path — episodic/monologue only)
**Deterministic alternative:** `gen_script_narration.py` for `continuous_narration`, `illustrated_narration`, `documentary`
**Model:** sonnet (episodic/monologue), none (narration formats)
**Reads:** `episode_direction.txt`, `projects/{{PROJECT_SLUG}}/VoiceCast.json`, `{{EPISODE_DIR}}/StoryPrompt.json`, `contracts/schemas/Script.v1.json`
**Writes:** `{{EPISODE_DIR}}/Script.json`

**Validates against:** `contracts/schemas/Script.v1.json`

**Required top-level fields:** `schema_id`, `schema_version`, `script_id`, `title`, `cast[]`, `scenes[]`

**Critical bindings (regression-sensitive):**
- `cast[].character_id` MUST match `VoiceCast.json` character IDs — Stage 5 looks up voice params by these IDs
- `cast[].gender` field is required — Stage 5 copies it into `AssetManifest.character_packs[].gender`
- `scenes[].dialogue[].speaker_id` MUST match a `cast[].character_id`
- Maximum 5 scenes

**Narration format deterministic path (`gen_script_narration.py`):**
- Reads `NarrationText.txt` + `pipeline_vars.sh` + VoiceCast.json
- Splits prose into sentences, groups into scenes via paragraph breaks → section dividers → N/3 fallback
- Produces a fully valid Script.v1.json with cast=[narrator], all actions type="dialogue", speaker_id="narrator"
- location="unspecified", time_of_day="unspecified", generic scene_ids (e.g. `s01e02_sc01`)
- Validates output against `contracts/schemas/Script.v1.json`

**Story format rules ({{STORY_FORMAT}} affects ALL of these):**
| Format | Script rule |
|--------|-------------|
| `episodic` | Multi-character dialogue, narrator for transitions |
| `continuous_narration` | ALL lines `speaker_id: "narrator"`, no scene divisions |
| `illustrated_narration` | ALL lines `speaker_id: "narrator"`, characters appear visually only |
| `documentary` | ALL lines `speaker_id: "narrator"`, topic-driven sections |
| `monologue` | ALL lines use protagonist's character_id (NOT "narrator") |

---

### Stage 4 — Produce ShotList.json
**File:** `prompts/p_4.txt` (LLM path — episodic/monologue), `prompts/p_4_c.txt` (creative fill — narration formats)
**Deterministic scaffold:** `gen_shotlist_scaffold.py` for `continuous_narration`, `illustrated_narration`, `documentary`, `ssml_narration`
**Model:** sonnet (all paths)
**Reads:** `{{EPISODE_DIR}}/Script.json`, `projects/{{PROJECT_SLUG}}/VoiceCast.json`, `contracts/schemas/ShotList.v1.json`
**Writes:** `{{EPISODE_DIR}}/ShotList.json`

**Validates against:** `contracts/schemas/ShotList.v1.json`

**Required fields per shot:** `shot_id`, `scene_id`, `duration_sec`, `camera_framing`, `camera_movement`, `characters[]`, `background_id`, `audio_intent`

**Required audio_intent sub-fields:**
- `sfx_tags[]` — list of sound effect descriptions
- `music_mood` — one phrase or null
- `vo_speaker_id` — character_id or null
- `vo_text` — verbatim dialogue or null
- `vo_item_ids[]` — IDs like `"vo-<scene_id>-<NNN>"` (EVERY VO line assigned to exactly one shot)
- `sfx_item_ids[]` — IDs like `"sfx-{shot_id}-{NNN}"` (parallel with sfx_tags)
- `music_item_id` — `"music-{shot_id}"` or null

**Critical invariants:**
- Total episode duration must be 60–120 seconds
- Every VO line in Script.json must appear in exactly one shot's `vo_item_ids`
- `timing_lock_hash` = SHA256 of sorted shot_ids list — used by render pipeline to detect shot list changes
- `vo_item_ids` format: `"vo-{scene_id}-{NNN}"` where `{scene_id}` is the EXACT `scenes[].scene_id` from Script.json — do NOT invent a descriptive slug
- **VO count validation (built into prompt):** after writing ShotList.json, p_4.txt runs a Python assertion: `total vo_item_ids across all shots == total dialogue actions in Script.json`. If it fails, the LLM must fix and retry before finishing Stage 4.

**Narration format scaffold+fill path (`gen_shotlist_scaffold.py` → `p_4_c.txt` → `validate_scaffold.py`):**
1. `gen_shotlist_scaffold.py` reads Script.json + VoiceCast.json, assigns all vo_item_ids, computes VO-anchored durations (with min 5s merge / max 45s split), computes `timing_lock_hash`, writes `ShotList_scaffold.json` with 7 creative `__FILL__` fields per shot (background_id, camera_framing, camera_movement, sfx_tags, music_mood, sfx_item_ids, music_item_id)
2. `fill_and_run "4_c"` sends scaffold to LLM via `p_4_c.txt` — LLM fills ONLY the `__FILL__` fields, overwrites `ShotList.json`
3. `validate_scaffold.py` verifies no residual `__FILL__` tokens + no pre-filled field drift

**Story format rules:**
| Format | Shot rule |
|--------|-----------|
| `continuous_narration` | `characters: []` (empty) on all shots |
| `documentary` | `characters: []` (empty) on all shots |
| `monologue` | Single protagonist, always `position: "center"` |
| `illustrated_narration` | Characters in `characters[]` but no VO assigned to them |

**Duration rules (added alongside character/format rules):**
- `continuous_narration` / `documentary` / `illustrated_narration`: `duration_sec` must be VO-anchored — derived from word count ÷ effective_wpm (adjusted for `azure_rate` read from VoiceCast.json) + break padding + 2 s buffer. Do NOT assign a cinematic/creative duration that ignores VO length.
- `episodic` / `monologue`: keep creative duration; soft constraint: `duration_sec ≤ estimated_VO_time + 8 s`
- VoiceCast.json must be read at Stage 4 to compute `effective_wpm`; it is pre-embedded by `inline_files_into_prompt` via the "Read these files" list

---

### Stage 5 — Produce AssetManifest drafts
**File:** `prompts/p_5.txt` (LLM path — episodic/monologue), `prompts/p_5_c.txt` (creative fill — narration formats)
**Deterministic scaffolds:** `gen_manifest_structure.py` (shared manifest) + `gen_vo_manifest.py` (per-locale VO manifests) for narration formats
**Model:** sonnet (all paths)
**Reads:** `{{EPISODE_DIR}}/ShotList.json`, `{{EPISODE_DIR}}/Script.json` (for `cast[].gender`), `projects/{{PROJECT_SLUG}}/VoiceCast.json`, `episode_direction.txt`, `contracts/schemas/AssetManifest.v1.json`
**Writes:**
- `{{EPISODE_DIR}}/AssetManifest_draft.shared.json` — locale-free (images, SFX, music)
- `{{EPISODE_DIR}}/AssetManifest_draft.{locale}.json` — per-locale VO manifests

**Validates against:** `contracts/schemas/AssetManifest.v1.json`

**Shared manifest structure:**
```
locale_scope: "shared"
vo_items: []              ← always empty
character_packs: [...]    ← one per unique character in ShotList
backgrounds: [...]        ← one per unique background_id in ShotList
sfx_items: [...]          ← derived from sfx_item_ids in ShotList
music_items: [...]        ← derived from music_item_id in ShotList
```

**Locale manifest (en) structure:**
```
locale_scope: "locale"
locale: "en"
shared_ref: "AssetManifest_draft.shared.json"
character_packs: []        ← always empty (lives in shared)
backgrounds: []            ← always empty (lives in shared)
background_overrides: []   ← always empty at draft time (post_tts_analysis fills this)
vo_items: [...]            ← one per VO line, with full tts_prompt
```

**New background fields (for media search multi-dim CLIP scoring):**
Each `backgrounds[]` entry now includes (all optional, generated by p_5.txt):
```
scoring_hints:      {subjects, environment, style, motion, technical}  ← drives multi_dim CLIP mode
search_queries:     ["search string 1", "search string 2", ...]
cinematic_role:     "establishing" | "transitional" | "reaction" | "climax" | "ambient"
motion_level:       "none" | "low" | "medium" | "high"
lighting:           free text (e.g. "warm golden hour")
include_keywords:   ["keyword1", ...]
exclude_keywords:   ["keyword2", ...]
continuity_hints:   {group_id, palette_target, motion_budget, camera_energy, allowed_shift}
```
- `scoring_hints` triggers `mode: multi_dim` in the media server scorer (5-dimensional CLIP scoring)
- Without `scoring_hints`, scorer falls back to `mode: fallback` (single ai_prompt CLIP score)
- `continuity_hints.group_id` groups items for the DP sequence ranker
- These fields are defined in `contracts/schemas/AssetManifest.v1.json` (9 new properties on backgrounds)
- `batch_store.create()` MUST use `**item_data` spread to preserve all these fields — do NOT cherry-pick

**Critical bindings:**
- `character_packs[].gender` comes from `Script.json cast[].gender` — NOT guessed
- `vo_items[].item_id` must be copied EXACTLY from `ShotList.json vo_item_ids[]` — do NOT rephrase or abbreviate
- `vo_items[].text` comes from `Script.json scenes[].actions[].line` (individual dialogue lines, in order) — NOT from `ShotList.shots[].audio_intent.vo_text` which is only a summary
- `vo_items[].tts_prompt.azure_voice` must match `VoiceCast.json character[en].azure_voice`
- `vo_items[].tts_prompt.azure_style` must be chosen from `VoiceCast.json character[en].available_styles` (fallback chain applies)
- `vo_items[].tts_prompt.azure_style_degree` copied EXACTLY from VoiceCast.json — do NOT per-line override
- `vo_items[].tts_prompt.azure_rate` copied EXACTLY from VoiceCast.json — do NOT per-line override
- DO NOT add `start_sec`, `end_sec`, `duration_sec` to vo_items — those are written by `post_tts_analysis.py`

**Story format rules:**
- `continuous_narration`, `documentary`: `character_packs: []` — no character images
- All other formats: character_packs generated normally

**Whisper special case:** when `azure_style = "whispering"`, set `azure_break_ms = 0` (pauses sound robotic in whispers)

**Narration format scaffold+fill path (`gen_manifest_structure.py` → `p_5_c.txt` → `validate_scaffold.py` → `gen_vo_manifest.py`):**
1. `gen_manifest_structure.py` reads Script.json + ShotList.json, builds `AssetManifest_draft.shared.json` with all deterministic fields pre-filled and 8 creative `__FILL__` fields per background (visual description, search queries, scoring hints, etc.)
2. `fill_and_run "5_c"` sends scaffold to LLM via `p_5_c.txt` — fills visual/search fields in backgrounds[] + sfx_items[] tags, overwrites shared manifest in-place
3. `validate_scaffold.py --warn-only` checks for residual `__FILL__` tokens and pre-filled drift (warn-only because LLM may legitimately restructure some visual fields)
4. `gen_vo_manifest.py` runs per-locale: reads Script.json + ShotList.json + VoiceCast.json, produces `AssetManifest_draft.{locale}.json` with fully-built vo_items (azure_voice, azure_style with fallback chain, azure_rate, azure_style_degree)

---

### Stage 6 — Write canon_diff.json
**File:** `prompts/p_6.txt` (LLM path — no longer used directly), `prompts/p_6.txt` (creative fill)
**Deterministic scaffold:** `canon_diff_chars.py` (extracts characters, produces `canon_diff_partial.json`)
**Model:** haiku (creative fill for narrative fields only)
**Reads:** `episode_direction.txt`, `{{EPISODE_DIR}}/Script.json`, `{{EPISODE_DIR}}/ShotList.json`, `projects/{{PROJECT_SLUG}}/canon.json` (optional), `contracts/schemas/CanonDiff.v1.json`
**Writes:** `{{EPISODE_DIR}}/canon_diff.json`

**Validates against:** `contracts/schemas/CanonDiff.v1.json`

**Structure:**
```json
{
  "episode": "s01e01",
  "added_characters": [...],     ← NEW characters only
  "updated_states": [...],       ← existing characters whose status changed
  "new_world_facts": [...],
  "new_unresolved_threads": [...],
  "resolved_threads": [...]
}
```

**Scaffold+fill path (`canon_diff_chars.py` → `p_6.txt` → `validate_scaffold.py`):**
1. `canon_diff_chars.py` reads Script.json cast + canon.json characters, computes `added_characters` (new) and `updated_states` (changed status/location), writes `canon_diff_partial.json` with `__FILL__` markers for `new_world_facts`, `new_unresolved_threads`, `resolved_threads`
2. Idempotency guard: if `canon_diff.json` already exists with no `__FILL__` tokens, exits 0 (skips)
3. `fill_and_run "6"` sends partial to LLM via `p_6.txt` — fills narrative fields only, writes complete `canon_diff.json`
4. `validate_scaffold.py` verifies no residual `__FILL__` tokens + no pre-filled character field drift

---

### Stage 7 — Update canon.json
**File:** `prompts/p_7.txt` (LLM path — no longer used)
**Deterministic replacement:** `canon_merge.py` (Python, all formats)
**Model:** none (deterministic)
**Reads:** `{{EPISODE_DIR}}/canon_diff.json`, `projects/{{PROJECT_SLUG}}/canon.json` (may not exist), `contracts/schemas/Canon.v1.json`
**Writes:** `projects/{{PROJECT_SLUG}}/canon.json` (created or updated)

**Validates against:** `contracts/schemas/Canon.v1.json`

**Merge rules:**
- If canon.json didn't exist: create fresh from diff
- If it existed: append new characters, update states, append world_facts (no duplicates), merge threads, append episode_summaries entry for this episode_id
- Prefers `episode_summary` from `canon_diff.json` if present; falls back to template "{episode_id}: See Script.json for details"

---

### Stage 8 — Produce locale variants
**File:** `prompts/p_8.txt`
**Model:** sonnet
**Reads:** All Stage 2–7 outputs for en locale + `VoiceCast.json` + `contracts/schemas/AssetManifest.v1.json`
**Writes:** For each non-en locale in LOCALES:
- `{{EPISODE_DIR}}/StoryPrompt.{locale}.json`
- `{{EPISODE_DIR}}/Script.{locale}.json`
- `{{EPISODE_DIR}}/ShotList.{locale}.json`
- `{{EPISODE_DIR}}/AssetManifest_draft.{locale}.json`
- `{{EPISODE_DIR}}/canon_diff.{locale}.json`
- `projects/{{PROJECT_SLUG}}/canon.{locale}.json`

**EARLY EXIT: if LOCALES is "en" only**, prints skip message and exits without writing anything.

**Locale AssetManifest structure (critical — different from shared/en):**
```json
{
  "locale_scope": "locale",
  "locale": "zh-Hans",
  "shared_ref": "AssetManifest_draft.shared.json",
  "character_packs": [],        ← ALWAYS empty
  "backgrounds": [],            ← ALWAYS empty
  "background_overrides": [],   ← ALWAYS empty at draft time
  "vo_items": [...],            ← translated VO with locale voice params
  "sfx_items": NOT INCLUDED,
  "music_items": NOT INCLUDED
}
```

**Critical invariants for Stage 8:**
- Story format guard: `continuous_narration`/`documentary` → NO `character_packs`, translate narrator VO only
- Voice replacement: look up `VoiceCast.json character[<locale>]` fields for each vo_item
- azure_style fallback chain (in order): angry→shouting→unfriendly→serious→(omit), fearful→terrified→whispering→sad→(omit), cheerful→excited→friendly→hopeful→(omit), etc.
- For DragonHD voices (whose available_styles are archetype names like "assassin", "captain"): use `VoiceCast.json character[locale].azure_style` when no emotion fallback matches
- Duration Matching Rule: translated text should take same time to speak as English (±20%)
- **`item_id` must be copied byte-for-byte from `AssetManifest_draft.en.json`** — Stage 8 produces a brand-new locale manifest but must NOT invent new item IDs. WAV filenames = `{item_id}.wav`; any deviation means TTS writes to a different filename than gen_render_plan.py looks up.
- DO NOT include `start_sec`, `end_sec` in translated vo_items
- DO NOT translate: `search_prompt`, `ai_prompt`, `motion.description`, any `*_id` field

---

### p_9.txt — SUPERSEDED (replaced by gen_render_plan.py in Stage 9)
**File:** `prompts/p_9.txt` — still present on disk but NOT invoked by the current pipeline.
**NOTE:** `gen_render_plan.py` (Stage 9[6/8]) does the same job deterministically without LLM calls.
`p_9.txt` referenced the old single-locale `AssetManifest.media.json` path — incompatible with
current multi-locale manifest structure. Do NOT re-enable p_9.txt in run.sh.

---

## PART 5 — STAGE 9 (formerly Stage 10): PYTHON POST-PROCESSING SCRIPTS

Stage 9 runs entirely without LLM calls. Invoked by `run_stage_10()` in `run.sh`
(function name kept for backward compat; the pipeline stage number is now 9).
Iterates over each locale in `${LOCALES}` (comma-separated, trimmed).

### Web UI step numbering vs run.sh labels

The web UI shows 11 numbered steps; run.sh uses [N/8] labels. They do not align 1-to-1:

```
Web UI [1]  = run.sh [1/8]  gen_music_clip       (shared, locale-free)
Web UI [2]  = web UI only   gen_characters        (shared; triggered via /run_step API)
Web UI [3]  = web UI only   gen_backgrounds       (shared; triggered via /run_step API)
Web UI [4]  = web UI only   gen_sfx               (shared; triggered via /run_step API)
             run.sh [1b/8]  music_prepare_loops   (shared, auto-runs after gen_music_clip)
             run.sh [2/8]   manifest_merge        (per locale)
Web UI [5]  = run.sh [2/8]  manifest_merge        (per locale)
Web UI [6]  = run.sh [3/8]  gen_tts_cloud         (per locale)
             run.sh [3b/8]  polish_locale_vo      (per non-primary locale)
Web UI [7]  = run.sh [4/8]  post_tts_analysis     (per locale)
             run.sh [4b/8]  music review checkpoint / apply_music_plan  (per locale)
Web UI [8]  = run.sh [4b/8] apply_music_plan      (per locale, if not NO_MUSIC)
Web UI [9]  = run.sh [5/8]  resolve_assets        (per locale)
Web UI [10] = run.sh [6/8]  gen_render_plan       (per locale)
Web UI [11] = run.sh [7/8]  render_video          (per locale)
             run.sh [8/8]   export_youtube_dubbed (per non-primary locale)
```

**Sub-step [1/8] — gen_music_clip.py** (locale-free, runs once)
```
Input:  projects/{slug}/episodes/{ep_id}/AssetManifest_draft.shared.json
Output: projects/{slug}/episodes/{ep_id}/assets/music/{item_id}.wav
        (one clip per music_items[] entry in the shared manifest)
```
- Reads MOOD tags from source files in `projects/resources/music/`
- Uses CLAP model to match `music_mood` text to best music segment
- Skips gracefully if no resources dir or no music_items
- Called with `|| true` — failure is non-fatal; Stage 9 continues without music clips
- Requires: `pip install laion-clap mutagen librosa soundfile numpy scipy`
- Checkpoint auto-downloaded to `~/.cache/laion_clap/` on first run

**Sub-step [1b/8] — music_prepare_loops.py** (locale-free, runs once, auto after [1/8])
```
Input:  projects/{slug}/episodes/{ep_id}/assets/music/{item_id}.wav  (from gen_music_clip)
        projects/{slug}/resources/music/*.mp3  (source tracks)
Output: projects/{slug}/episodes/{ep_id}/assets/music/music_loop_candidates.json
        (BPM, duration, candidate loop points per source track)
```
- Called with `|| true` — failure is non-fatal
- Provides metadata for the Music tab Source Browser in the web UI

**Sub-step [2/8] — manifest_merge.py** (per locale)
```
Input:  --shared  AssetManifest_draft.shared.json
        --locale  AssetManifest_draft.{locale}.json
Output: AssetManifest_merged.{locale}.json  (locale_scope: "merged")
```
- Merges shared + locale manifest into single resolved view
- Computes `duck_intervals` per shot from VO `start_sec`/`end_sec` + `music.fade_sec`
- Computes a per-locale `timing_lock_hash`
- Requires: stdlib only

**Sub-step [3/8] — gen_tts_cloud.py** (per locale)
```
Input:  AssetManifest_merged.{locale}.json  (reads vo_items[].tts_prompt)
Output: assets/{locale}/audio/vo/{item_id}.wav
        assets/{locale}/audio/vo/licenses/{item_id}.license.json
        assets/meta/gen_tts_cloud_results.json
        assets/meta/tts_audit_log.json          ← appended each run
```
- Calls Azure Cognitive Services Speech SDK
- Requires env vars: `AZURE_SPEECH_KEY`, `AZURE_SPEECH_REGION`
- Uses SSML with: `<voice>`, `<mstts:express-as style= styledegree=>`, `<prosody rate= pitch=>`, `<break time='Nms'/>`
- **Null-style rule:** when `azure_style` is null or empty, `<mstts:express-as>` is omitted entirely — bare `<voice>` + `<prosody>` is valid SSML; Azure rejects `style=""` attribute
- Voice selection priority: `azure_voice` field > `character_packs[].gender` > `voice_style` > hardcoded default

**Synthesis mode selection (`_select_synthesis_mode`):**
- `batch_bookmark` (default): all voices in whitelist (standard Neural, ≤ BATCH_MAX items) → one multi-speaker SSML call with `<bookmark>` events; WAV split by byte offsets
- `chunk_alignment`: any HD/Dragon voice, OR episode too large for batch → synthesises N chunks in parallel; uses CTC forced aligner (ctc_forced_aligner) for per-sentence timing, falls back to silence-gap detection; yields full instrumentation log
- `per_item_legacy`: single-item `--asset-id` re-synthesis OR last-resort fallback → one Azure call per item; slowest path
- `run()` returns a **3-tuple** `(results, chunks_meta, stats)` — callers MUST unpack all three; `chunks_meta` is `[]` for non-chunk modes

**TTS cache (`assets/meta/tts_cache/`):**
- SHA256-keyed WAV cache shared across all runs; key = `normalized_ssml + voice + locale + output_format + sample_rate + style_engine_version`
- Cleared by `_purge_episode_assets()` (Purge Cache checkbox). Deleting WAV files alone does NOT clear the cache — Azure calls will NOT be re-issued unless the cache is also cleared.

**`--force` flag:**
- Bypasses the existing-WAV skip in `_synthesise_per_item()` — overwrites existing WAVs unconditionally
- Required for `polish_locale_vo.py` re-synthesis loop to actually converge (without `--force`, every iteration measures the same stale WAV duration)

**`tts_audit_log.json` (`assets/meta/tts_audit_log.json`):**
- Appended by `_append_tts_audit_log()` after every `main()` run (including partial `--asset-id` runs)
- Accumulates per-locale totals: `total_raw_chars`, `total_ssml_chars`, `total_api_calls`, `total_items_synthesized`, `total_items_skipped`, `runs`
- Each entry records: `timestamp`, `locale`, `mode`, `raw_chars`, `ssml_chars`, `ssml_ratio`, `api_calls`, `cache_hits`, `chunks`, `items_synthesized`, `items_skipped`, `wall_time_sec`
- Use this to verify actual Azure character consumption across re-runs. Cache hits do not add to `api_calls` or `ssml_chars`.

- **Batch path (default):** builds one multi-speaker SSML per locale with `<bookmark mark="{item_id}"/>` as the FIRST CHILD INSIDE each `<voice>` block (NOT as a sibling before `<voice>`); single `speak_ssml_async()` call returns one WAV; `BookmarkReached` events give per-item byte offsets; WAV is split into individual `{item_id}.wav` files. `--asset-id` flag bypasses batch and uses per-item path
- **SSML bookmark placement is critical:** `<bookmark>` must be inside `<voice>`: `<voice name='...'><bookmark mark='id'/>{text}</voice>`. Azure error 1007 ("RootSpeak should not contain node [bookmark]") fires if bookmark is placed as direct child of `<speak>`.
- **Azure SDK EventSignal:** `synthesizer.bookmark_reached` uses `.connect(cb)` and `.disconnect_all()` — there is NO `.disconnect(cb)` method. Calling `disconnect(cb)` in a `finally` block raises `AttributeError` even when synthesis succeeded, causing every batch call to fall back to per-item.
- **Stale WAV cleanup:** before synthesis, `gen_tts_cloud.py` deletes any `.wav` in the locale vo dir whose stem is not in the current manifest's `item_id` set. This prevents re-runs from silently reusing audio from a previous run with different item IDs.
- Requires: `pip install azure-cognitiveservices-speech>=1.38.0`

**Sub-step [3b/8] — polish_locale_vo.py** (per non-en locale only)
```
Input:  AssetManifest_merged.{locale}.json
        assets/{locale}/audio/vo/{item_id}.wav  (zh WAVs just synthesized)
        assets/en/audio/vo/{item_id}.wav         (EN reference WAVs)
Output: AssetManifest_merged.{locale}.json   (patched with rewritten text)
        AssetManifest_draft.{locale}.json    (patched in sync)
        vo_alignment.{locale}.json           (per-item before/after ratios)
        prompts/tts_calibration.{locale}.json (chars/sec history, ≤20 entries)
```
- Measures zh_dur/en_dur ratio for every VO line; flags lines outside [THRESHOLD, THRESHOLD_HIGH]
- THRESHOLD = 0.90, THRESHOLD_HIGH = 1.10 — **single source of truth: `polish_locale_vo.py`**
- Lines below THRESHOLD get `direction="expand"`; lines above THRESHOLD_HIGH get `direction="shorten"`
- Calls `claude -p --model sonnet` in batches of BATCH_SIZE (default 15) with 300 s timeout per batch
- MUST unset `CLAUDECODE` env var before spawning the claude subprocess (nested sessions are blocked)
- After Claude rewrites, patches both merged and draft manifests, then calls `gen_tts_cloud.py --asset-id --force` per patched item to re-synthesize; repeats up to MAX_ITERS (default 3)
- **`--force` is mandatory in re-synthesis calls:** without it, `gen_tts_cloud.py` skips existing WAVs unconditionally → every iteration measures the same unchanged duration → ratio never improves → loop runs MAX_ITERS with zero progress. The `--force` flag is passed explicitly by `resynthesize_items()`.
- **Stale WAV guard in gen_tts_cloud.py --asset-id mode:** uses `all_manifest_ids` (all vo_items in the full manifest) for stale cleanup — NOT the single-item filtered set. This prevents `--asset-id` calls from deleting every other item's WAV.
- **Timeline Lock interaction:** when locale is non-primary, the primary locale's shot durations floor the non-primary durations. If `polish_locale_vo` cannot converge (ratio stays outside [0.90, 1.10] after MAX_ITERS), the non-primary WAV will be truncated by the render — there is no error; the video just cuts the audio short within the shot.
- Skipped entirely if locale == "en" or if EN WAV directory doesn't exist
- Requires: stdlib only (claude CLI must be on PATH; AZURE_SPEECH_KEY needed for re-synthesis)

**Sub-step [4/8] — post_tts_analysis.py** (per locale)
```
Input:  AssetManifest_draft.{locale}.json  (mutates IN PLACE)
        assets/{locale}/audio/vo/{item_id}.wav  (measures duration)
Output: AssetManifest_draft.{locale}.json  (adds start_sec, end_sec to vo_items[])
        Also writes background_overrides[] for overflow shots
```
- Measures actual WAV durations with soundfile
- Computes sequential VO placement: `start_sec[0]=0`, `start_sec[n+1]=end_sec[n]+0.3`
- Detects overflow when total VO duration > shot `duration_sec`
- Overflow handling: writes `background_overrides[]` with extended `duration_sec` + `_music_item.duration_sec` patches
- **Must run BEFORE manifest_merge.py for accurate duck_intervals** (but run.sh runs it after; see ordering note below)
- Requires: `pip install soundfile`

**Sub-step [4b/8] — apply_music_plan.py** (per locale, skipped if NO_MUSIC=1)
```
Input:  projects/{slug}/episodes/{ep_id}/MusicPlan.json  (if confirmed via Music tab)
        assets/music/{item_id}.wav  (clips from gen_music_clip)
Output: assets/music/{item_id}_loop.wav  (looped clips to cover shot duration)
        (also writes per-shot applied clips if overrides present)
```
- Preceded by a music review checkpoint: if MusicPlan.json exists → applies it;
  if not → skips silently (music_items in shared manifest stay as-is)
- `shot_overrides[].music_asset_id` may be a source stem (e.g. "djokovic_bg_climax")
  rather than an existing item_id; apply_music_plan extracts a new clip from the source
  MP3 in that case
- **`track_volumes`** (MusicPlan top-level, optional): per-source-track dB offset (−18 to 0).
  Applied AFTER `apply_shot_overrides()` so `music_asset_id` is populated on manifest items.
  Writes `base_db = BASE_MUSIC_DB + offset` onto matching `music_items[]`.
  Skips any item that already has `base_db` set (per-shot override takes priority).
  Unmatched stems produce a non-fatal warning.
- **`clip_volumes`** (MusicPlan top-level, optional): per-clip dB offset (−18 to 0); dual-key strategy:
  - `item_id` key (e.g. `"music-s01e01_sh01"`) — auto clips; applied directly to matching manifest item
  - `clip_id` key (e.g. `"djokovic_bg_calm:19.4s-40.3s"`) — user-cut clips; resolved via
    `shot_overrides[].music_clip_id` at apply time (persisted by clip_id immediately, resolved to
    item_id when assigned to a shot)
  Both key types coexist in the same dict. Items with `base_db` already set are skipped.
- **Priority chain (highest → lowest):** `shot_overrides[].base_db` > `clip_volumes` > `track_volumes` > `BASE_MUSIC_DB` (−6.0 dB)
- `BASE_MUSIC_DB = -6.0` defined locally in apply_music_plan.py (same constant duplicated in
  gen_render_plan.py and render_video.py; future cleanup: move to shared constants module)
- Skipped entirely when `NO_MUSIC=1` env var is set

**Sub-step [5/8] — resolve_assets.py** (per locale)
```
Input:  AssetManifest_merged.{locale}.json
        assets/media/selections.json  (optional — from VC editor Media tab)
Output: AssetManifest.media.{locale}.json
```
- run.sh auto-detects `selections.json` and passes `--selections <path>` if it exists
- When selections are present, maps stock media URLs/paths to background asset_ids
- v3 selections (multi-segment): emits one item per segment with `segment_index`, `duration_sec`, `hold_sec` fields
- Probes local filesystem for each asset — no network calls
- Emits `file://` URIs for found files, `placeholder://` for missing
- Asset path conventions:
  - VO:         `assets/{locale}/audio/vo/{item_id}.{wav|mp3|ogg}`
  - Music:      `assets/music/{item_id}.{wav|mp3|ogg}`
  - SFX:        `assets/sfx/{item_id}.{wav|mp3|ogg}`
  - Background: `assets/{asset_id}.{ext}` (then `assets/backgrounds/` subdir)
  - Character:  `projects/{project_id}/characters/` (project-level first), then `assets/characters/`, then `assets/` root; also tries stripping `char-` prefix and `-vN` suffix
- Requires: stdlib only

**Sub-step [6/8] — gen_render_plan.py** (per locale)
```
Input:  --manifest  AssetManifest_merged.{locale}.json
        --media     AssetManifest.media.{locale}.json
Output: AssetManifest_final.{locale}.json
        RenderPlan.{locale}.json
```
- Deterministic replacement for p_9.txt LLM stage
- VO `timeline_in_ms`/`timeline_out_ms` come from measured `start_sec`/`end_sec` (NOT evenly distributed)
- `duration_ms` respects `background_overrides` for overflow shots
- `timing_lock_hash` comes from `AssetManifest_merged` (locale-adjusted)
- Passes `duck_intervals`/`duck_db`/`fade_sec`/`base_db` into each shot's music entry;
  `base_db` is read from the music_item (written by apply_music_plan.py); falls back to
  `BASE_MUSIC_DB` (−6.0) if absent. Without this threading, `base_db` written by
  apply_music_plan.py is silently dropped before reaching render_video.py.
- **VO_TAIL_MS floor (all formats):** `duration_ms = max(duration_ms, last_vo_out_ms + 2000)` — every shot gets at least 2 s after the final spoken word regardless of format. Applied before the ceiling.
- **Narrative-format ceiling (narrative formats only):** for `continuous_narration`/`documentary`/`illustrated_narration`, `duration_ms` is further capped at `last_vo_out_ms + 2000` — floor and ceiling converge to the same value, pinning duration exactly. Music item `end_ms` and `duck_interval` endpoints are also clamped to the corrected duration.
- **VO completeness guard:** after building `shotlist_vo_ids`, any `vo_item` in the merged manifest that has a valid `shot_id` but is NOT in any shot's `vo_item_ids` is auto-injected into that shot before building `vo_lines`. Prints `[VO-INJECT]` log. Protects against Stage 4 LLM silently dropping the last line of a scene from `vo_item_ids`.
- Requires: stdlib only

**Sub-step [7/8] — render_video.py** (per locale)
```
Input:  --plan    RenderPlan.{locale}.json
        --locale  {locale}
        --out     renders/{locale}/
        --profile preview_local | draft_720p | high
        --no-music (if NO_MUSIC=1)
Output: renders/{locale}/output.mp4
        renders/{locale}/output.srt
        renders/{locale}/render_output.json
```
- Per-shot MKV intermediates in `.shots/`, then concat → output.mp4
- Multi-segment backgrounds (v3): uses FFmpeg `concat` filter to stitch multiple clips/images per shot — no `-stream_loop` seam artifacts; falls back to single-bg loop for v1/v2
- Character compositing: 1=center, 2=left/right thirds, 3=even thirds, 4+=quarters
- Active speaker: scale×1.05 opacity 1.0; inactive: scale×0.95 opacity 0.80
- Audio mixing: VO at 0 dB, music at `base_db` dB un-ducked (default −6 dB; per-track or
  per-clip volume offset applied via `base_db` field in shot dict), SFX at −3 dB
- `build_duck_expr()` receives `base_db` from `shot.get("base_db", BASE_MUSIC_DB)`; `duck_db`
  is multiplicative relative to `base_db` (`duck_amp = base_amp * 10^(duck_db/20)`), so existing
  duck values are preserved unchanged when `base_db` shifts
- Loudnorm: -16 LUFS (linear=true) applied in concat pass
- Music continuity: same `music_asset_id` across shots = seamless; different = hard cut
- n_chars == 0 case (continuous_narration/documentary): no character compositing; verified handled at line ~401
- Requires: FFmpeg in PATH

**Sub-step [8/8] — export_youtube_dubbed.py** (per non-en locale only)
```
Input:  renders/{locale}/output.mp4
Output: renders/{locale}/youtube_dubbed.aac
```
- Extracts mixed audio track as standalone AAC (192 kbps, 48 kHz, stereo)
- Skipped for `en` locale (primary upload uses the video's own audio)
- Requires: FFmpeg in PATH

### CRITICAL ORDERING NOTE for Stage 9
The sub-steps in run.sh run in this order (shared steps first, then per-locale loop):
```
── SHARED (locale-free, run once) ─────────────────────────────────────────────
[1/8]   gen_music_clip       ← || true (non-fatal)
[1b/8]  music_prepare_loops  ← || true (non-fatal); auto-follows [1/8]

── PER-LOCALE LOOP (repeats for each locale in ${LOCALES}) ────────────────────
[2/8]   manifest_merge       ← runs BEFORE TTS; duck_intervals empty on first run
[3/8]   gen_tts_cloud        ← generates WAVs; caches in assets/meta/tts_cache/
[3b/8]  polish_locale_vo     ← non-primary locale only; rewrites out-of-ratio lines,
                                re-synthesizes with gen_tts_cloud --asset-id --force
[4/8]   post_tts_analysis    ← measures WAVs, writes start_sec/end_sec into draft manifest
[4b/8]  music review checkpoint / apply_music_plan
                             ← skipped if NO_MUSIC=1; applies MusicPlan.json if confirmed
[5/8]   resolve_assets       ← maps stock media URLs; needs selections.json if media was chosen
[6/8]   gen_render_plan      ← deterministic; replaced p_9.txt LLM stage
[7/8]   render_video         ← per-shot MKV → concat → output.mp4 + output.srt
[8/8]   export_youtube_dubbed← non-primary locale only; extracts AAC audio track
```

**Web UI step numbers ([1]–[11])** displayed in the progress panel:
```
[1]  gen_music_clip      (shared)
[2]  gen_characters      (shared, web-UI-triggered via /run_step)
[3]  gen_backgrounds     (shared, web-UI-triggered via /run_step)
[4]  gen_sfx             (shared, web-UI-triggered via /run_step)
[5]  manifest_merge      (per locale)
[6]  gen_tts             (per locale — includes polish_locale_vo [3b/8] internally)
[7]  post_tts            (per locale)
[8]  apply_music_plan    (per locale — corresponds to [4b/8])
[9]  resolve_assets      (per locale)
[10] gen_render_plan     (per locale)
[11] render_video        (per locale — export_youtube_dubbed [8/8] follows automatically)
```

manifest_merge [2/8] runs BEFORE TTS [3/8] and post_tts_analysis [4/8], meaning on the
first run `start_sec`/`end_sec` don't exist yet and duck_intervals will be empty.
This is intentional — re-running from step [2/8] after step [4/8] produces accurate duck intervals.

---

## PART 6 — WORKFLOW C: VOICE CAST EDITOR

### Files involved
`code/http/test_server.py` — JS functions: `vcOpen()`, `vcSave()`, `vcContinue()`, `buildVcCard()`, presetSel change handler
API endpoints: `GET /api/voice_index`, `GET /api/voice_presets`, `GET /api/azure_voices`, `POST /api/save_voice_cast`, `POST /api/delete_preset`, `POST /api/preview_voice`

### Flow
1. After Stage 0 completes (or existing episode loaded), user opens Voice Cast editor
2. `vcOpen()` loads `VoiceCast.json` into `_vcData` (in-memory)
3. UI renders one card per character per locale
4. User can preview voice clips via "▶ Play" button
5. User can apply presets (stored per voice in a presets dir)
6. User saves edits → `vcSave()` → `POST /save_voice_cast` → writes `VoiceCast.json`
7. User clicks "Continue" (optional) → `vcContinue()` → calls `startPipeStep` to run stages 1–N

### Voice presets
- Source: `GET /api/voice_presets?voice=<azure_voice_name>`
- Files: `projects/resources/voice_presets/{voice_name}/*.json`
- Each preset: `{style, style_degree, rate, pitch, break_ms, hash, label}`
- "— no preset —" selection: restores `_voiceIndex[voice].clips[style].params` defaults (does NOT early-return without resetting)

### Voice catalog files
- `prompts/azure_tts_styles.txt` — **single file** used by BOTH Stage 0 (pre-embedded) AND `/api/azure_voices` (VC editor)
  - 275 voices: styled voices (62) + no-style en/zh-capable voices + bridge voices + multitalker section
  - Pre-embedded by `inline_files_into_prompt` into Stage 0 prompts
  - Parsed by `parse_azure_tts_styles()` in `test_server.py` for the VC editor dropdown
- There is NO separate `azure_tts_styles_full.txt` — both Stage 0 and VC editor use the same file

### Voice picker (two-tier UI)
- Default view: shows only styled voices (`has_styles === true`) — same 62 as before expansion
- "Show all voices" toggle: reveals full catalog grouped by locale (`<optgroup>`)
- `has_styles` bool annotated by `parse_azure_tts_styles()`, included in `/api/azure_voices` response

### parse_azure_tts_styles() parser safety
The parser uses `VOICE_RE` (matches `[STANDARD]` or `[DRAGON]` tier bracket), `LOCALE_RE`, `STYLES_RE`, `SUPPORTS_RE`.
New content added by `build_voice_catalog.py` is silently safe:
- Section header lines (`====== EN-CAPABLE ======`, etc.) — do not match `VOICE_RE` → ignored ✓
- `supports(N): [...]` lines — matched by `SUPPORTS_RE` → stored in `entry["supports"]` ✓
- All 275 voice entries follow the same format as existing entries → parse correctly ✓
- `styles(0): []` voices parse as `styles: []` → `has_styles = False` → filtered out of default VC picker ✓
- `is_multitalker` set to `True` if "ultitalker" in voice name (case-insensitive) ✓
Do NOT change `VOICE_RE` to match section headers — the tier bracket anchor is intentional.

### VO Editor State Machine

#### Triggers & Parameter Reset Rules

| # | Trigger | Params Action | Preset Dropdown | Source |
|---|---------|--------------|-----------------|--------|
| 1 | Voice name changed → same as saved VoiceCast voice | Restore saved VoiceCast.json params | Auto-select matching preset (if any) | `charLoc.*` |
| 2 | Voice name changed → different voice | Reset to index defaults → universal fallback | Reset to "— no preset —" | `_vcGetDefaults(voice, "")` |
| 3 | Style changed (any style, including "— no style —") | Always reset to defaults | Reset to "— no preset —" | `_vcGetDefaults(voice, style)` |
| 4 | Custom preset selected from dropdown | Load preset's saved params | Shows selected preset | `preset.*` |
| 5 | "— no preset —" selected from dropdown | Reset to defaults for current style | Stays on "— no preset —" | `_vcGetDefaults(voice, style)` |

Key rule: **Style changes NEVER auto-load custom presets.** Custom preset params only load when the user explicitly selects one from the Custom dropdown.

#### Preview & Persistence Rules

| # | Scenario | Index Action | Preset Action |
|---|----------|-------------|---------------|
| 6 | Preview with canonical default params, voice+style NOT in index | Create index entry | — |
| 7 | Preview with canonical default params, voice+style already in index | — (hash matches) | — |
| 8 | Preview with custom params, voice+style NOT in index | Do NOT create index entry | Save as custom preset |
| 9 | Preview with custom params, voice+style already in index | — (index untouched) | Save as custom preset (if hash is new) |
| 10 | Preview with params matching an existing preset | — | — (hash dedup, no duplicate) |

Key rule: **Index.json only accepts canonical default params.** Non-default params are always saved as presets, never pollute the index.

#### Canonical Default Values

| Context | degree | rate | pitch | break_ms |
|---------|--------|------|-------|----------|
| Styled clip (any named style) | 1.0 | 0% | "" | 0 |
| No-style clip ("— no style —") | 1.0 | 0% | -5% | 600 |

#### Default Lookup Priority (`_vcGetDefaults`)

1. Look up `_voiceIndex[voice].clips[style]` → return stored params
2. If not found → return universal fallback (table above)

#### Hash Key Formula (must be identical in ALL tools)

```
sha256(json.dumps({"v":voice, "l":azure_locale, "s":style||"",
                   "d":degree, "r":rate, "p":pitch||"", "b":break_ms,
                   "t":sample_text}, sort_keys=True))[:16]
```

Used in: `test_server.py /api/preview_voice`, `pre_cache_voices.py cache_key()`, `index.json hash`, `presets.json hash`.
8-field key — the `"l"` (azure_locale) field is REQUIRED. Never omit it or the hashes will silently diverge.

#### Delete Custom Preset

Select a custom preset → click **Delete** button → removes entry from `presets.json` + deletes cached `.mp3/.wav` file via `POST /api/delete_preset`.

### Regression risks for Voice Cast
1. When user selects "— no preset —", the Emotion/Speak Speed/Pitch/Break fields MUST reset to defaults via `_vcGetDefaults()`. The old bug was `if (!preset) return;` skipping this reset — FIXED.
2. When style changes, params MUST reset to defaults. The old bug was auto-loading the most recent custom preset for that style — FIXED. Presets only load via explicit dropdown selection.
3. Voice change handler MUST restore saved VoiceCast.json params when switching back to the saved voice. The old bug was unconditionally resetting to role-based defaults — FIXED.
4. Hash key_dict MUST use 8 fields (including `"l": azure_locale`) in ALL tools. The old bug was `pre_cache_voices.py` using 7 fields (no locale) while `test_server.py` used 8 → hashes never matched → spurious custom presets on every Preview — FIXED.
5. Index.json MUST only store entries with canonical default params. The old bug was blindly saving the first preview's params as the "default" regardless of values — FIXED.
6. `_voiceIndex` is loaded from `GET /api/voice_index` — must be loaded before presetSel change handler fires.
7. `vcContinue()` builds `ep_dir` from `currentSlug`/`currentEpId` — both must be set before voice cast can be opened for a running episode.
8. Both Stage 0 AND `/api/azure_voices` read from the SAME `prompts/azure_tts_styles.txt` — do NOT introduce a separate `_full.txt` unless Stage 0 haiku token budget becomes a problem after expansion to 275 entries (~17–22K tokens).

---

## PART 7 — WORKFLOW D: EXISTING PROJECT LOAD

### Files involved
`code/http/test_server.py` — JS functions: `loadRunProjects()`, `onRunProjectChange()`, `onRunEpisodeChange()`, `loadEpisodeDetails()`
API endpoints: `GET /list_projects`, `GET /pipeline_status`

### Flow
1. User selects a project from `run-project-sel` dropdown
2. `onRunProjectChange()` called:
   - Populates `run-episode-sel` with episode IDs from `_runProjectList`
   - Sets `_usingExistingEp = true`
   - Calls `loadEpisodeDetails(slug, firstEpId)`
3. `loadEpisodeDetails(slug, epId)`:
   - Sets `currentSlug = slug; currentEpId = epId;` **immediately on first line (synchronous)**
   - Calls `GET /pipeline_status?slug=...&ep_id=...`
   - Calls `syncRunTabFromPipeline(...)` to update UI state

### Regression risks
1. `loadEpisodeDetails` sets `currentSlug`/`currentEpId` synchronously on its first line — this is why `setRunBtnEnabled(true)` in `onRunProjectChange` is safe to call before the async fetch completes
2. `onRunProjectChange()` assumes `_runProjectList` is already populated. If a new episode was just created but `loadRunProjects()` hasn't been called since, the episode dropdown may miss it
3. `GET /list_projects` returns `{slug, episodes: [{id, files: [...]}]}` — any change to this format breaks the episode dropdown population in `onRunProjectChange`

---

## PART 7b — WORKFLOW E: MEDIA SEARCH & SELECTION

### Files involved

**Media search server** (runs on NFS server 192.168.86.33:8200):
- `code/media/http/server.py` — FastAPI server, `POST /batches`, `GET /batches`, `GET /batches/{id}`
- `code/media/http/downloader.py` — Pexels/Pixabay image+video fetchers, populates `_thumbnails` dict
- `code/media/http/scorer.py` — CLIP scoring: `mode: fallback` (single prompt) or `mode: multi_dim` (5 dimensions)
- `code/media/http/batch_store.py` — Persistent batch state to `batch_state.json` on disk
- `code/media/http/sequence_ranker.py` — DP-based continuity ranker using pairwise discontinuity
- `code/media/http/config.json` — Scoring profiles, thresholds (NO API keys — keys come from env vars)
- `code/media/http/requirements.txt` — Self-contained deps (install torch CPU first, then this file)

**VC editor proxy** (runs on 192.168.86.40:8080):
- `code/http/test_server.py` — Media tab JS + three proxy routes + selections.json writer

### Environment variables

| Variable | Where | Purpose |
|----------|-------|---------|
| `MEDIA_API_KEY` | Both servers | Internal auth key (user-defined, e.g. `internal-test-key`). **Server-side only on VC editor — NEVER exposed to browser** |
| `PEXELS_API_KEY` | Media server | Pexels stock photo/video API key |
| `PIXABAY_API_KEY` | Media server | Pixabay stock photo/video API key |
| `MEDIA_SERVER_URL` | VC editor | Pre-fills the media server URL input in the UI via `{{MEDIA_SERVER_URL}}` placeholder |

### Step-by-step flow

**Step 1 — User opens Media tab, selects episode**
- JS: `initMediaTab()` populates episode dropdown from `GET /list_projects`
- `onMediaEpChange()` sets `_mediaSlug`, `_mediaEpId`
- Calls `mediaLoadExisting()` to check for a previously completed batch

**Step 2 — User clicks 🔍 Search Media**
- JS: `mediaStartSearch()` → `POST /api/media_batch` with `{slug, ep_id, server_url}`
- VC editor proxy:
  1. Reads `AssetManifest_draft.shared.json` for the selected episode
  2. Injects `MEDIA_API_KEY` from `os.environ` into `X-Api-Key` header
  3. Forwards full manifest as `POST /batches` to the media server
- Media server:
  1. Extracts `backgrounds` from manifest (auto-converts list→dict by `asset_id` if needed)
  2. `batch_store.create()` stores batch state with `**item_data` spread (preserves ALL fields)
  3. Background task: downloads images/videos, CLIP scores, ranks

**Step 3 — Polling**
- JS: `_mediaPoll()` fires every 3 s via `setInterval`
- Route: `GET /api/media_batch_status?batch_id=...&server_url=...`
- VC editor proxy injects `MEDIA_API_KEY` server-side, forwards to `GET /batches/{id}`
- Continues until `status == "done"` or `status == "failed"`

**Step 4 — Results rendered**
- JS: `_mediaRenderResults()` builds thumbnail grid per background item
- Images: `<img>` with `loading='lazy'`; Videos: `<video preload='none'>` with `dataset.lazySrc`
- Videos loaded via `IntersectionObserver` → `_mediaLazyLoad()`, throttled to max 2 concurrent
- Hover: `mouseenter` restores `src` from `dataset.lazySrc` and calls `play()`; `mouseleave` calls `_mediaStopVid()` which removes `src` and calls `load()` to release the HTTP connection
- Only 1 video can stream at a time (`_mediaPlayingVid` guard)
- `file://` URLs rewritten to `/api/serve_media_file?url=...` proxy
- If `recommended_sequence` is present, ⚡ Apply Recommended Sequence button shown

**Step 5 — User selects media (two options)**
- **Manual**: click thumbnails to select per-item → stored in `_mediaSelections`
- **⚡ Apply Recommended Sequence**: `mediaApplyRecommended()` auto-selects from `sequence_ranker.py` output
  - Only available when `continuity_hints` present in manifest (new format)
  - Uses DP ranker considering color/light/motion/semantic discontinuity across shots
  - User can override individual items after applying

**Step 6 — ✔ Confirm Selections**
- JS: `mediaConfirm()` calls `_mediaReleaseAllConnections()` first (drains lazy queue, aborts all actively-downloading videos) to guarantee a free HTTP connection slot
- Then `POST /api/media_confirm` with `{slug, ep_id, batch_id, version, selections}`
- Server writes `{ep_dir}/assets/media/selections.json`
- This file is read by run.sh (Stage 5 hard stop check) and `resolve_assets.py` (Stage 9[5/8])

### CLIP scoring modes

| Mode | Triggered by | How it works |
|------|-------------|--------------|
| `fallback` | No `scoring_hints` in item (old manifest) | Single `ai_prompt` → one CLIP cosine score |
| `multi_dim` | `scoring_hints` dict present (new manifest) | 5 dimensions: subjects, environment, style, motion, technical — weighted average |

### Regression risks for media workflow
1. `batch_store.create()` MUST use `**item_data` spread — cherry-picking 3 fields was the root cause of `mode: fallback` despite `scoring_hints` being present in the manifest
2. `_mediaServerUrl()` JS regex must be `/\/+$/` (one backslash) — NOT `/\\/+$/` (two backslashes) — raw string escaping issue causes `SyntaxError: Unexpected token ','` which cascades to `switchTab is not defined`
3. `MEDIA_API_KEY` must only be read from `os.environ` in the three proxy routes — do NOT add it to HTML, JS, or query params
4. `{{MEDIA_SERVER_URL}}` replacement happens at HTML serve time in `do_GET` — must match the placeholder in the `<input>` value attribute
5. Media tab JS polling (`setInterval`) continues running when user switches to other tabs (tabs are CSS visibility, not page navigation)
6. `selections.json` must be written before Stage 9 `resolve_assets.py` runs — enforced by the Stage 5 hard stop
7. **HTTP/1.1 6-connection-per-host limit**: video thumbnails, SSE streams, pollers, and fetch requests all share the same 6-slot budget. `_mediaLazyLoad` throttle MUST stay ≤ 2 concurrent. `_mediaStopVid()` MUST NOT re-set `vid.src` after clearing it (per HTML spec, setting `.src` triggers the load algorithm and opens a new HTTP connection even with `preload='none'`). `_mediaReleaseAllConnections()` MUST be called before `mediaConfirm()` POST.
8. **`/api/serve_media_file` streams in 256 KB chunks** — does NOT read entire files into RAM. `BrokenPipeError`/`ConnectionResetError` are caught and silently ignored (browser closes connection after getting metadata).

---

## PART 8 — WEB SERVER API ENDPOINTS REFERENCE

All endpoints implemented in `code/http/test_server.py`. Server runs on `localhost:8080`.

### GET endpoints
| Path | Parameters | Returns |
|------|-----------|---------|
| `/stream` | `ep_dir`, `from`, `to`, `test`, `profile`, `no_music` | SSE stream of run.sh output |
| `/run_step` | `step`, `slug`, `ep_id`, `locale`, `profile`, `no_music` | SSE stream of single post-proc step |
| `/run_locale` | `slug`, `ep_id`, `locale`, `profile`, `no_music`, `purge` (default=1), `from` | SSE stream of all post-proc steps for locale |
| `/run_stage10` | `slug`, `ep_id`, `from`, `profile`, `no_music`, `purge` (default=1) | SSE stream of shared Stage 9 steps then all-locale steps (endpoint name kept for backward compat) |
| `/list_projects` | — | `[{slug, episodes: [{id, files:[]}]}]` |
| `/pipeline_status` | `slug`, `ep_id` | `{story_file, voice_cast, stages: {N: bool}}` |
| `/view_artifact` | `slug`, `ep_id`, `artifact` | JSON content of named artifact file |
| `/api/check_slug` | `slug` | `{exists: bool, suggested: slug}` |
| `/api/next_episode_id` | `slug` | **`{"next_ep_id": "s01e01"}`** ← key is `next_ep_id` |
| `/api/voice_index` | — | Voice index JSON from `projects/resources/voices/index.json` |
| `/api/voice_presets` | `voice` | Array of presets for that voice |
| `/api/azure_voices` | — | Parsed `prompts/azure_tts_styles.txt` catalog; each entry includes `has_styles: bool` and `is_multitalker: bool` for client-side filtering |
| `/api/status_report` | `slug`, `ep_id` | `{text}` — full contents of `status_report.txt` for that episode (empty string if file doesn't exist) |
| `/serve_media` | `path` | Binary file content (for audio preview) |
| `/api/media_batches` | `slug`, `ep_id`, `server_url` | Proxied list of batches from media server |
| `/api/media_batch_status` | `batch_id`, `server_url` | Proxied batch status/results from media server |
| `/api/serve_media_file` | `url` | Binary file content — proxies `file://` URIs for NFS media (browsers block file:// from http:// pages) |
| `/api/vo_locales` | `ep_dir` | `{locales: ["en", "zh-Hans", ...]}` — scans ep_dir for `AssetManifest_merged.*.json` files |
| `/api/vo_items` | `ep_dir`, `locale` | `{items: [...]}` — returns `vo_items` from merged manifest, each annotated with `duration_sec` from WAV on disk |

### POST endpoints
| Path | Body | Returns |
|------|------|---------|
| `/stop` | — | Kills running subprocess |
| `/save_story` | `{story}` | `{filename, num}` |
| `/delete_episode_dir` | `{path}` | `{ok}` |
| `/api/infer_story_meta` | `{story}` | `{title, slug, genre, story_format, metadata_found[], slug_exists, slug_suggested}` |
| `/api/create_episode` | `{slug, ep_id, story, title, genre, story_format, locales, no_music}` | `{ok, ep_dir, slug, ep_id}` |
| `/save_voice_cast` | `{slug, voice_cast_json}` | `{ok}` |
| `/api/append_status_report` | `{slug, ep_id, text}` | `{ok}` — appends timestamped entry to `status_report.txt` |
| `/api/media_batch` | `{slug, ep_id, server_url}` | `{batch_id, poll_url}` — reads AssetManifest_draft.shared.json, forwards to media server |
| `/api/media_confirm` | `{slug, ep_id, batch_id, version, selections}` | `{ok, path, n}` — writes selections.json (version: 1=single, 2=per-shot, 3=multi-segment per shot) |
| `/api/episode_file` | `?slug=X&ep_id=Y&file=ShotList.json` | raw JSON — whitelisted: ShotList.json, selections.json |
| `/api/retune_vo` | `{slug, ep_id, locale, dry_run, backup, items:[{item_id, text?, azure_style?, azure_rate?, azure_pitch?, azure_style_degree?, azure_break_ms?}]}` | `{results:[{item_id, status, before_duration_sec, after_duration_sec, duration_delta_sec, duration_warn, wav_path, error}]}` |

### Key invariants for API endpoints
- `/api/next_episode_id` returns key `"next_ep_id"` — JS client reads `nd.next_ep_id`
- `/stream` expects `ep_dir=` param (relative path from PIPE_DIR), NOT `story_file=`
- `/stream` reads `no_music` from `meta.json` if the checkbox is not checked (fallback from meta)
- `/stop` terminates ALL running procs for the current client address
- `/delete_episode_dir` only allows deletion inside `projects/<slug>/episodes/<id>/` (depth ≥ 4 safety check)

### Purge cache endpoint behavior

`_purge_episode_assets(slug, ep_id, locale="")` is a Python helper called from `/run_locale` and
`/run_stage10` when `purge=1` (the default):

- **Per-locale purge** (`locale="zh-Hans"`, from `/run_locale`): deletes the target locale's WAVs
  (`assets/{locale}/audio/vo/*.wav`), locale image assets, renders (`output.mp4`, `output.srt`,
  `render_output.json`), merged manifest, media manifest, and render plan. Shared assets are
  untouched.
- **All-locale purge** (`locale=""`, from `/run_stage10`): discovers all locales from
  `AssetManifest_draft.*.json` filenames, purges each locale as above, PLUS deletes shared music
  WAVs, shared background/character images, and `assets/meta/gen_tts_cloud_results.json`.
- After purge, `force_run = True` is set for that invocation, bypassing all "already done"
  checkpoint skips for every step in the loop.
- The **🗑 Purge Cache checkbox** (`id="chk-purge-assets"`, JS global `purgeAssets = true`)
  defaults to **checked**. Both `/run_locale` and `/run_stage10` URL builders append
  `&purge=1` when checked, `&purge=0` when unchecked.
- Purpose: prevent stale WAVs (from a previous run using different `item_id` names) or stale
  images from being silently reused when the pipeline is re-run after an LLM stage changed.

---

## PART 9 — CROSS-CUTTING INVARIANTS (must hold after ANY modification)

### 1. Placeholder substitution chain
Every `{{PLACEHOLDER}}` token used in any `prompts/p_N.txt` MUST have a corresponding sed rule in `fill_and_run()` in `run.sh`. Current complete list:
```
{{EPISODE_DIR}}    ← must be first, as other paths are derived from it
{{STORY_FILE}}
{{PROJECT_SLUG}}
{{EPISODE_ID}}
{{EPISODE_NUMBER}}
{{STORY_TITLE}}
{{SERIES_GENRE}}
{{GENERATION_SEED}}
{{RENDER_PROFILE}}
{{LOCALES}}
{{STORY_FORMAT}}
```
**If you add a placeholder to any prompt, you MUST add its sed rule to run.sh.**
**Note:** `ssml_narration` does not introduce any new placeholders — it reuses existing `{{STORY_FORMAT}}` and skips LLM stages 1, 2, 3, 8 entirely.

### 2. character_id binding chain
The character identifier flows through 5 stages and must be identical at each step:
```
VoiceCast.json characters[].character_id
  → Stage 3: Script.json cast[].character_id  (copied from VoiceCast, not invented)
  → Stage 4: ShotList.json shots[].audio_intent.vo_speaker_id
  → Stage 5: AssetManifest vo_items[].speaker_id
  → Stage 5: AssetManifest character_packs[].character_id
  → gen_tts_cloud.py: looks up voice params from VoiceCast.json by speaker_id
```
Breaking any link in this chain causes voice assignment to fall back to gender defaults or fail entirely.

### 3. item_id cross-reference chain
Shot audio item IDs are assigned in Stage 4 and must be referenced consistently:
```
ShotList.shots[].audio_intent.vo_item_ids[] = ["vo-<scene_id>-<NNN>"]
  → Stage 5: AssetManifest vo_items[].item_id must match these IDs exactly
  → Stage 8: locale AssetManifest vo_items[].item_id must copy from en manifest byte-for-byte
  → gen_tts_cloud.py: writes {item_id}.wav
  → gen_render_plan.py: joins on item_id to build RenderPlan.shots[].vo_lines[]

ShotList.shots[].audio_intent.sfx_item_ids[] = ["sfx-{shot_id}-{NNN}"]
  → Stage 5: AssetManifest sfx_items[].item_id must match

ShotList.shots[].audio_intent.music_item_id = "music-{shot_id}" | null
  → Stage 5: AssetManifest music_items[].item_id must match
```

**VO completeness guard (gen_render_plan.py):** If a vo_item exists in the merged manifest with a valid `shot_id` but is absent from the ShotList's `vo_item_ids`, gen_render_plan.py auto-injects it into the correct shot and prints `[VO-INJECT]`. This is a safety net — Stage 4's VO count validation should prevent the gap from occurring in the first place.

### 4. Manifest split invariant
- `AssetManifest_draft.shared.json`: `locale_scope: "shared"`, contains character_packs, backgrounds, sfx_items, music_items; `vo_items: []` (always empty)
- `AssetManifest_draft.{locale}.json`: `locale_scope: "locale"`, contains ONLY vo_items; NO character_packs/backgrounds/sfx_items/music_items
- `AssetManifest_merged.{locale}.json`: `locale_scope: "merged"`, contains ALL sections merged by manifest_merge.py

### 5. post_tts_analysis writes; no LLM writes
- `start_sec` and `end_sec` on `vo_items[]` are ONLY written by `post_tts_analysis.py`
- No LLM stage (p_0 through p_9) should write these fields
- `background_overrides[]` is ONLY written by `post_tts_analysis.py`
- `duck_intervals` are initially computed by `manifest_merge.py`; `gen_render_plan.py` may clamp their endpoints for narrative-format shots when the shot duration is shortened by the ceiling rule — this is intentional and not a violation of this invariant

### 6. VoiceCast.json is append-only per project
- Stage 0 reads VoiceCast.json first, then only adds characters not already present
- Existing character entries are NEVER modified by Stage 0
- This preserves voice consistency across all episodes of the same project
- VoiceCast.json lives at `projects/{slug}/VoiceCast.json` — project-level, NOT episode-level

### 7. run.sh ep_dir interface
The `/stream` endpoint passes `ep_dir=` to `run.sh` as arg 1.
`run.sh` takes: `./run.sh <ep_dir> [from_stage] [to_stage]`
**The old interface** (`./run.sh story_N.txt`) **is completely removed.**
Any code that passes `story_file=` to `/stream`, or that builds a command like `./run.sh story_N.txt`, is broken.

### 8. Stage 9 script execution order within a locale
Must NOT be reordered:
```
[1]  gen_music_clip    (shared) → writes assets/music/*.wav
[2]  manifest_merge             → reads locale manifest (start_sec may be absent on first run)
[3]  gen_tts_cloud              → writes assets/{locale}/audio/vo/*.wav
[3b] polish_locale_vo (non-en) → rewrites zh lines outside [0.90,1.10]; re-synthesizes; updates manifests
[4]  post_tts_analysis          → mutates locale manifest: adds start_sec/end_sec; writes background_overrides
[5]  resolve_assets             → reads merged manifest + selections.json → AssetManifest.media.{locale}.json
[6]  gen_render_plan            → reads merged + media → AssetManifest_final + RenderPlan
[7]  render_video               → reads RenderPlan → output.mp4
[8]  export_youtube_dubbed      → reads output.mp4 → youtube_dubbed.aac (skipped for en)
```

### 9. Media API key security
- `MEDIA_API_KEY` is NEVER exposed to the browser — no `<input>`, no JS variable, no query param
- All three media proxy routes (`/api/media_batch`, `/api/media_batches`, `/api/media_batch_status`) inject the key server-side via `os.environ.get("MEDIA_API_KEY", "")`
- The key is sent to the media server in the `X-Api-Key` HTTP header
- Do NOT re-add a `media-api-key` input element to the HTML

### 10. Media selections.json path
- Canonical path: `{ep_dir}/assets/media/selections.json`
- Written by: VC editor `POST /api/media_confirm`
- Read by: run.sh (hard stop check), resolve_assets.py (`--selections` flag)
- Format v1 (single per bg): `{batch_id, slug, episode_id, version: 1, selections: {item_id: {media_type, url, path, score}}}`
- Format v2 (per-shot): `{batch_id, slug, episode_id, version: 2, selections: {item_id: {per_shot: {shot_id: {media_type, url, path, score}}}}}`
- Format v3 (multi-segment per shot): `{batch_id, slug, episode_id, version: 3, selections: {item_id: {per_shot: {shot_id: {segments: [{media_type, url, path, score, duration_sec, hold_sec}, ...]}}}}}`
- `resolve_assets.py` detects `per_shot` key to differentiate v1 vs v2/v3, and `segments` key within per_shot entries for v3 — all three formats must work
- Per-shot items emitted by `resolve_assets.py` include a `shot_id` field in the output item
- v3 segment items emitted by `resolve_assets.py` additionally include `segment_index`, `duration_sec`, and `hold_sec` fields
- `gen_render_plan.py` uses composite key `"bg_id:shot_id"` for per-shot media lookup, falls back to `"bg_id"` for background-level (v1); multi-segment items (with `segment_index`) are grouped in `_segments` dict keyed as `"bg_id:shot_id"` → sorted list

### 11. Media server backgrounds format compatibility
- Media server `POST /batches` accepts `manifest.backgrounds` as both list (old) and dict (new)
- List format: auto-converted to dict using `asset_id` as key (server.py line 233–234)
- `batch_store.create()` must use `**item_data` spread — do NOT cherry-pick individual fields

### 12. file:// URI proxy for NFS media
- Media server writes images/videos to NFS-shared storage using `file://` URIs
- Browsers block `file://` from `http://` pages — VC editor provides `/api/serve_media_file?url=<encoded>` proxy
- JS `_mediaThumb()` rewrites `file://` URLs to proxy URLs before setting `img.src` / `vid.src`

### 13. VC editor config file (`code/http/config.json`)
- Priority chain: env var > config.json > hardcoded default
- `MEDIA_TOP_N` env var → `config.media.top_n` → `5` (hardcoded)
- `MEDIA_SERVER_URL` env var → `config.media.default_server_url` → `""` (hardcoded)
- Config loaded once at module level into `_vc_config` dict
- Sent to media server via `POST /batches` `top_n` field and to browser via `{{MEDIA_SERVER_URL}}` placeholder

### 14. Per-shot media selection (multi-segment v3)
- JS fetches `ShotList.json` via `GET /api/episode_file` to build `_mediaShotMap` (bg_id → [shot_id, ...]) and `_mediaShotDur` (shot_id → duration_sec)
- If ShotList not available (404), `_mediaShotMap = null` → single-selection mode (backward compatible)
- Per-shot mode (v3): clicking thumbnails appends a segment to the first shot with remaining gap; multiple clips/images stack until shot duration is filled
- ⚡ Auto-assign stacks candidates per shot: videos use probed `duration_sec`, images auto-fill remaining gap with `hold_sec`
- Duration badge: video thumbnails show `duration_sec` badge (top-left, e.g. "12.3s") when the media server provides it
- Shot rows show: expected duration, fill progress bar, segment list with ✕ remove, gap indicator
- `GET /api/episode_file` whitelisted to `ShotList.json` and `selections.json` only
- `mediaConfirm()` sends `version: 3` when any selection has segments, `version: 2` when per-shot without segments, `version: 1` when single-selection
- `POST /api/media_confirm` passes through `version` into `selections.json`

### 15. Media server video duration (FFprobe)
- `scorer.py` `score_videos()` calls `_probe_duration()` via FFprobe for each video before scoring
- `duration_sec` field is always present on video results (float or null if probe failed)
- `server.py` `_add_urls()` copies `duration_sec` through to the batch response automatically (via `dict(r)`)
- Images do not have `duration_sec` (field is absent, not null)

### 16. Multi-segment background rendering
- `gen_render_plan.py` `build_shot()` populates `background_segments[]` in RenderPlan shots when v3 segment items are found in the media map
- `background_segments` is a list of `{asset_id, uri, media_type, duration_sec, hold_sec}` or `null` (for v1/v2 single-background)
- Primary `background_asset_id` and `background_media_type` are set from the first segment for backward compat
- `render_video.py` uses FFmpeg `concat` filter for multi-segment backgrounds (NOT `-stream_loop`) — each video segment is trimmed to its `duration_sec`, each image segment is held for `hold_sec`
- When `background_segments` is `null` or has only 1 entry, render_video.py falls through to existing single-background logic (loop/hold as before)
- Backward compat: v1/v2 selections.json and old RenderPlans (no `background_segments`) work unchanged

---

## PART 10 — SCHEMA REFERENCE

All schemas in `contracts/schemas/`. Claude validates artifacts using `python -c "import jsonschema; jsonschema.validate(...)"`.

| Schema file | Used by | Key required fields |
|-------------|---------|---------------------|
| `StoryPrompt.v1.json` | Stage 2 | schema_id, schema_version, prompt_id, episode_goal, generation_seed, series.{title,genre,tone}, setting.primary_location, characters[≥2], constraints.max_scenes |
| `Script.v1.json` | Stage 3 | schema_id, schema_version, script_id, title, cast[], scenes[] |
| `ShotList.v1.json` | Stage 4 | schema_id, schema_version, shotlist_id, script_ref, timing_lock_hash, shots[] |
| `AssetManifest.v1.json` | Stage 5, 8 | schema_id, schema_version, manifest_id, project_id, episode_id, locale_scope; backgrounds[]: 9 new optional properties (scoring_hints, search_queries, cinematic_role, motion_level, lighting, include_keywords, exclude_keywords, continuity_hints, source_filters) |
| `CanonDiff.v1.json` | Stage 6 | episode, added_characters[], updated_states[], new_world_facts[], new_unresolved_threads[], resolved_threads[] |
| `Canon.v1.json` | Stage 7 | project_id, characters{}, world_facts[], unresolved_threads[], episode_summaries[] |
| `AssetManifest_final.v1.json` | gen_render_plan | schema_id, schema_version, manifest_id, project_id, shotlist_ref, items[] |
| `RenderPlan.v1.json` | gen_render_plan | schema_id, schema_version, plan_id, project_id, timing_lock_hash, profile, shots[] |

---

## PART 11 — STORY FORMAT IMPACT MATRIX

When `STORY_FORMAT` is changed or a new format is added, these are ALL the places that must handle it:

| Stage/Script | What changes for non-episodic formats |
|---|---|
| `p_0.txt` (Stage 0) | `continuous_narration`/`illustrated_narration`/`documentary`: narrator ONLY; `monologue`: protagonist only, NO narrator entry |
| `run.sh` (Stage 0) | `continuous_narration`/`illustrated_narration`/`documentary`: runs `gen_pipeline_vars.py` + `voice_cast_narrator.py` (deterministic, no LLM); `ssml_narration`: runs `ssml_preprocess.py` |
| `run.sh` (Stage 1) | ALL formats: `canon_check.py` replaces LLM (deterministic) |
| `p_3.txt` (Stage 3) | `continuous_narration`/`illustrated_narration`/`documentary`: ALL `speaker_id: "narrator"`; `monologue`: protagonist's id only |
| `run.sh` (Stage 3) | `continuous_narration`/`illustrated_narration`/`documentary`: `gen_script_narration.py` (deterministic, no LLM); `ssml_narration`: skipped (ssml_preprocess.py already wrote Script.json) |
| `run.sh` (Stages 1,2,3,8) | `ssml_narration`: stages 1, 2, 3, 8 are SKIPPED entirely |
| `run.sh` (Stage 4) | `continuous_narration`/`illustrated_narration`/`documentary`/`ssml_narration`: scaffold+fill pattern (`gen_shotlist_scaffold.py` → `p_4_c.txt` → `validate_scaffold.py`); `episodic`/`monologue`: full LLM via `p_4.txt` |
| `p_4.txt` (Stage 4) | `continuous_narration`/`documentary`/`ssml_narration`: `characters: []` on all shots; `monologue`: protagonist always center; **all narrative formats**: `duration_sec` VO-anchored (word_count ÷ effective_wpm + breaks + 2 s buffer) — NOT creative guess; `ssml_narration`: 60-120s total constraint does NOT apply, use `pause_after_ms` from Script.json |
| `run.sh` (Stage 5) | `continuous_narration`/`illustrated_narration`/`documentary`/`ssml_narration`: scaffold+fill pattern (`gen_manifest_structure.py` → `p_5_c.txt` → `validate_scaffold.py` → `gen_vo_manifest.py`); `episodic`/`monologue`: full LLM via `p_5.txt` |
| `p_5.txt` (Stage 5) | `continuous_narration`/`documentary`/`ssml_narration`: `character_packs: []` — no character images |
| `run.sh` (Stage 6) | ALL formats: scaffold+fill pattern (`canon_diff_chars.py` → `p_6.txt` → `validate_scaffold.py`) |
| `run.sh` (Stage 7) | ALL formats: `canon_merge.py` replaces LLM (deterministic) |
| `run.sh` (p_9.txt) | ALL formats: p_9.txt skipped entirely (replaced by `gen_render_plan.py` in Stage 9[6/8]) |
| `p_8.txt` (Stage 8) | `continuous_narration`/`documentary`: skip character_packs translation, translate narrator VO only |
| `run.sh` (Stage 9[3/8]) | `ssml_narration`: passes `--ssml-narration --ssml-inner --voice-cast` to `gen_tts_cloud.py` for wrapper-rebuild + inner passthrough |
| `run.sh` (Stage 9[3b/8]) | `ssml_narration`: polish_locale_vo SKIPPED (authored SSML timing is immutable) |
| `run.sh` (Stage 9[8/8]) | `ssml_narration`: export_youtube_dubbed SKIPPED |
| `test_server.py` | `ssml_narration`: `/api/infer_story_meta` detects SSML tags via regex, returns `story_format: "ssml_narration"` without haiku call |
| `render_video.py` (Stage 9[7/8]) | `n_chars == 0` case (no character compositing) — handled at line ~401 |

**STORY_FORMAT flows via:**
`meta.json.story_format` → `pipeline_vars.sh export STORY_FORMAT=...` → `run.sh sed {{STORY_FORMAT}}` → prompt content → LLM behavior

**ssml_narration special flow:**
`meta.json.story_format=ssml_narration` → `pipeline_vars.sh` stub (written by createEpisode) → `run.sh` sources stub → Stage 0 dispatches to `ssml_preprocess.py` → stages 1,2,3,8 skipped → stages 4,5,6,7 run with LLM → Stage 9 uses `--ssml-narration` mode for TTS

---

## PART 12 — REGRESSION CHECKLIST

Run through the relevant items after any modification:

### After modifying prompts/p_N.txt
- [ ] Every `{{PLACEHOLDER}}` in the modified prompt has a sed rule in `run.sh`
- [ ] The file referenced in "Read these files" section still exists at that path
- [ ] The output file path still matches what downstream stages expect to read
- [ ] Schema validation call references the correct schema file
- [ ] Format-specific branches cover all 6 formats: episodic, continuous_narration, illustrated_narration, documentary, monologue, ssml_narration
- [ ] No new `start_sec`/`end_sec`/`duration_sec` fields added to vo_items (those belong to post_tts_analysis)

### After modifying run.sh
- [ ] All 11 placeholders still substituted ({{EPISODE_DIR}} through {{STORY_FORMAT}})
- [ ] Stage 9 sub-step sequence preserved: [1/8] gen_music_clip → [1b/8] music_prepare_loops → (per-locale:) [2/8] manifest_merge → [3/8] gen_tts → [3b/8] polish_locale_vo → [4/8] post_tts → [4b/8] apply_music_plan → [5/8] resolve_assets → [6/8] gen_render_plan → [7/8] render_video → [8/8] export_youtube_dubbed
- [ ] Stage 0 re-source of `pipeline_vars.sh` still happens after Stage 0 completes
- [ ] `run.sh` still accepts `<ep_dir> [from] [to]` signature (not story_file)
- [ ] `fill_and_run` still passes `--dangerously-skip-permissions --no-session-persistence` to claude
- [ ] Hard stop after Stage 5 checks `${EP_DIR}/assets/media/selections.json` — exits 0 if missing, continues if present
- [ ] Hard stop after Stage 8 always exits 0 when `TO_STAGE > 8` — unconditional pre-render pause
- [ ] `resolve_assets.py` call passes `--selections <path>` when `selections.json` exists on disk
- [ ] `ssml_narration` stage skip guard: stages 1, 2, 3, 8 print `⏭ skipped` and `continue` when `STORY_FORMAT=ssml_narration`
- [ ] `ssml_narration` Stage 0 branch: calls `ssml_preprocess.py "$EP_DIR"` instead of `fill_and_run 0`
- [ ] Narrator format Stage 0 branch: calls `gen_pipeline_vars.py "$EP_DIR"` + `voice_cast_narrator.py "$EP_DIR"` for `continuous_narration`/`illustrated_narration`/`documentary`
- [ ] Stage 1: always calls `canon_check.py "$EP_DIR"` (no `fill_and_run`)
- [ ] Stage 3 narration guard: calls `gen_script_narration.py "$EP_DIR"` for `continuous_narration`/`illustrated_narration`/`documentary`, `fill_and_run 3` for episodic/monologue
- [ ] Stage 4 narration guard: scaffold+fill (`gen_shotlist_scaffold.py` → `fill_and_run "4_c"` → `validate_scaffold.py`) for narration formats, `fill_and_run 4` for episodic/monologue
- [ ] Stage 5 narration guard: scaffold+fill (`gen_manifest_structure.py` → `fill_and_run "5_c"` → `validate_scaffold.py` → `gen_vo_manifest.py` per-locale) for narration formats, `fill_and_run 5` for episodic/monologue
- [ ] Stage 6: always scaffold+fill (`canon_diff_chars.py` → `fill_and_run "6"` → `validate_scaffold.py`)
- [ ] Stage 7: always calls `canon_merge.py "$EP_DIR"` (no `fill_and_run`)
- [ ] p_9.txt: always skipped (run.sh skips LLM Stage 9; gen_render_plan.py handles it in Stage 9[6/8])
- [ ] `get_stage_model()` strips `_c` suffix via `n_base="${n%%_*}"` before case match — `"5_c"` → `"5"` → sonnet
- [ ] `stage_label()` strips `_c` suffix, appends " (creative fill)" when suffix is `_c`
- [ ] `validate_scaffold.py` after `fill_and_run "4_c"` and `fill_and_run "6"`: strict mode (exit 1 on drift); after `fill_and_run "5_c"`: `--warn-only` (warn on drift)
- [ ] `code_dir` variable set before the stage loop: `code_dir="$(cd "$(dirname "$0")" && pwd)/code/http"`
- [ ] `ssml_narration` Stage 9[3/8]: passes `--ssml-narration --ssml-inner --voice-cast` to `gen_tts_cloud.py`
- [ ] `ssml_narration` Stage 9[3b/8]: polish_locale_vo skipped (guard: `STORY_FORMAT != ssml_narration`)
- [ ] `ssml_narration` Stage 9[8/8]: export_youtube_dubbed skipped (guard: `STORY_FORMAT != ssml_narration`)

### After modifying test_server.py
- [ ] `/stream` passes `ep_dir=` (not `story_file=`) to `run.sh`
- [ ] `runPrompt()` guards on `currentSlug && currentEpId` before building ep_dir
- [ ] `createEpisode()` calls `/api/create_episode` and sets `currentSlug`/`currentEpId` from response
- [ ] `vcContinue()` builds `ep_dir` from `currentSlug`/`currentEpId`
- [ ] `startPipeStep({type:'llm'})` passes `ep_dir=` to `/stream`
- [ ] `runPrepare()` reads `nd.next_ep_id` (NOT `nd.ep_id`) from `/api/next_episode_id` response
- [ ] Fallback episode ID is `'s01e01'` (NOT `'ep0001'`)
- [ ] `/api/next_episode_id` server handler returns JSON key `"next_ep_id"` (not `"ep_id"`)
- [ ] `presetSel` change handler — when "— no preset —" is selected — restores `_voiceIndex[voice].clips[style].params` defaults before returning (does NOT early-return without resetting)
- [ ] `loadEpisodeDetails` still sets `currentSlug = slug; currentEpId = epId;` on its FIRST line (before any await)
- [ ] `_pipeline_status()` Stage 1 done detection uses `stage_logs/{slug}.{ep_id}.stage_1.log` (size > 100), NOT `stage_1_check.txt` — do not revert this
- [ ] `stopRun()` calls **both** `hideStageProgress()` AND `pipeRunning = null` in addition to `setStatus('idle')`. There are two separate spinner elements: `spinnerEl` (run-tab status bar, hidden by `setStatus('idle')`) and `stage-progress-spinner` (stage progress bar, hidden only by `hideStageProgress()`). Calling only `setStatus('idle')` leaves the stage progress bar spinning after the user clicks Stop.
- [ ] `log_message(fmt, *args)` converts `args[0]` with `str()` before calling `.split()`. `send_error()` passes an `HTTPStatus` enum as the `code` argument — enums have no `.split()` method and crash without the explicit `str()` conversion.
- [ ] **Purge Cache flow**: JS global `purgeAssets` defaults to `true`; `chk-purge-assets` checkbox defaults to `checked` (with `accent-color:#e06c75` red accent); both `/run_locale` and `/run_stage10` URL builders append `&purge=1` or `&purge=0`; server-side handlers parse `purge` and call `_purge_episode_assets()` before the step loop when `purge=1`, then set `force_run = True`.
- [ ] **[R-001] Stages uncheck instantly on re-run**: When user clicks Run N→10 (e.g. Run 8→10), stages N–10 must uncheck INSTANTLY in the Pipeline tab — must NOT wait for the next server poll and must NOT leave stale ✓ from the previous run. Implemented via `_stagesDoneInCurrentRun` Set that is cleared at run-start and gates the checkmark rendering. Applies to all entry points: Run button, Pipeline tab re-run, alignment-warning re-run link. **Test**: click Run 8→10 on a fully-completed episode → stages 8, 9, 10 uncheck before the first output line appears.
- [ ] **`beforeunload` handler** cleans up ALL connections: closes `es` and `pipeStepEs` EventSources, clears `_pipePoller` and `_mediaPollTimer` intervals, calls `_mediaReleaseAllConnections()`, stops `_vcPlayingAudio`. Without this, reloading the tab after a server restart hangs because the browser's connection pool has stale dead sockets.
- [ ] **`switchTab()` connection cleanup**: clears stale EventSource refs (readyState === 2), stops playing video when leaving Media tab, stops voice-preview audio when leaving Run tab
- [ ] **`Handler.end_headers()` override** sends `Connection: close` on every response — prevents browser from pooling TCP connections that become stale after server restart
- [ ] **`ReusableServer.daemon_threads = True`** — SSE handler threads and video-streaming threads MUST be daemon threads so the server process exits cleanly on Ctrl-C (without this, zombie threads from long-lived SSE connections keep the old process alive, and the browser tab's reload hangs on the dead process)
- [ ] **`_mediaStopVid(vid)`** removes `src` and calls `load()` to abort the download, stores URL in `dataset.lazySrc` for later restore on hover. Does NOT re-set `vid.src` (that would open a new HTTP connection per HTML spec)
- [ ] **`_mediaReleaseAllConnections()`** called before `mediaConfirm()` — stops playing vid, drains lazy queue, aborts all actively-downloading video elements (readyState < 2 or not paused)
- [ ] **Video lazy-load concurrency** `_mediaLazyLoad` limit is 2 (NOT 4) — leaves headroom for SSE + pollers within the 6-connection HTTP/1.1 budget
- [ ] `/api/infer_story_meta` SSML pre-check: regex `<(?:speak|voice|prosody|mstts:)\b` on first 2000 chars → returns `story_format: "ssml_narration"` without haiku call; `else:` branch runs normal haiku extraction

### After modifying any Stage 9 Python script
- [ ] CLI arg names unchanged (`--manifest`, `--shared`, `--locale`, `--media`, `--plan`, `--out`, etc.)
- [ ] Output file paths still match what the next script reads as input
- [ ] `_build_step_cmd()` in `test_server.py` references correct script name and args
- [ ] `run_stage_10()` in `run.sh` still calls script with correct args
- [ ] `gen_tts_cloud.py` SSML: `<bookmark>` is inside `<voice>` block — do NOT move it outside
- [ ] `gen_tts_cloud.py` EventSignal: uses `.disconnect_all()` not `.disconnect(cb)` in finally block
- [ ] `gen_tts_cloud.py` stale WAV cleanup: uses `all_manifest_ids` (built from **full** `manifest.get("vo_items", [])`, NOT from the `--asset-id`-filtered `items` list) so that single-item re-synthesis calls do not delete WAVs belonging to other items
- [ ] `gen_tts_cloud.py` `--ssml-narration` mode: requires `--ssml-inner` and `--voice-cast`; calls `run_ssml_narration()` which rebuilds SSML wrappers from VoiceCast.json + passes through ssml_inner.xml content; injects `auto_sent_NNN` bookmarks; remaps to vo_item_ids; archives `_full_narration.wav`
- [ ] `gen_tts_cloud.py` `build_ssml_narration()`: bookmark placement inside `<voice>` block (same rule as `build_episode_ssml()`); prosody/style wrappers rebuilt from VoiceCast.json narrator entry
- [ ] `gen_tts_cloud.py` `run()` returns a **3-tuple** `(results, chunks_meta, stats)` — do NOT unpack as 2-tuple; `main()` does `results, chunks_meta, stats = run(...)` and calls `_append_tts_audit_log(meta_dir, locale, stats)` after
- [ ] `gen_tts_cloud.py` synthesis mode: `_select_synthesis_mode()` returns one of `batch_bookmark` / `chunk_alignment` / `per_item_legacy`; HD/Dragon voices always → `chunk_alignment`; single `--asset-id` always → `per_item_legacy`
- [ ] `gen_tts_cloud.py` `--force` flag: skips the existing-WAV guard in `_synthesise_per_item()`; required for `polish_locale_vo` re-synthesis to converge
- [ ] `gen_tts_cloud.py` `tts_audit_log.json`: appended after every `main()` run; accumulates `total_raw_chars`/`total_ssml_chars`/`total_api_calls` per locale; skipped items appear as `status: "skipped"` (NOT `"success"`)
- [ ] `polish_locale_vo.py` `resynthesize_items()`: passes `--force` to every `gen_tts_cloud.py --asset-id` call — without it the loop never converges
- [ ] `gen_render_plan.py` VO completeness guard: loop over `merged.get("vo_items")` injecting missing IDs still present before `build_shot()` calls
- [ ] `gen_render_plan.py` VO_TAIL_MS = 2000 still applied as floor: `duration_ms = max(duration_ms, vo_end_ms + VO_TAIL_MS)`
- [ ] `gen_render_plan.py` `build_media_map()` composite key: per-shot items keyed as `"bg_id:shot_id"`, background-level items keyed as `"bg_id"`, multi-segment items (with `segment_index`) grouped in `"_segments"` dict
- [ ] `gen_render_plan.py` `build_shot()` bg lookup: tries `_segments["bg_id:shot_id"]` first (multi-segment), then `"bg_id:shot_id"` (per-shot v2), then `"bg_id"` (v1)
- [ ] `gen_render_plan.py` `build_shot()` sets `background_segments` in rendered shot (list or None)
- [ ] `resolve_assets.py` backgrounds use `bg.get("asset_id") or bg["item_id"]` — backgrounds use `item_id` key, not `asset_id`
- [ ] `resolve_assets.py` `_load_selections()` detects `per_shot` key → resolves each shot entry independently via `_resolve_one_entry()`; detects `segments` key within per_shot entries for v3 multi-segment
- [ ] `resolve_assets.py` per-shot items include `item["shot_id"] = shot_id` — this field enables composite key lookup in `gen_render_plan.py`
- [ ] `resolve_assets.py` v3 segment items additionally include `segment_index`, `duration_sec`, and `hold_sec` fields

### After modifying deterministic stage scripts (code/http/canon_check.py, canon_merge.py, gen_script_narration.py, gen_pipeline_vars.py, voice_cast_narrator.py, canon_diff_chars.py, gen_shotlist_scaffold.py, gen_manifest_structure.py, gen_vo_manifest.py, validate_scaffold.py)
- [ ] Script reads `pipeline_vars.sh` via same parse logic (regex `^(?:export\s+)?([A-Z_]...)`) — do NOT use `source` or `eval`
- [ ] `canon_check.py` output is always > 100 bytes (Stage 1 done detection depends on log size)
- [ ] `canon_merge.py` is append-only for characters, world_facts, threads — never removes existing canon entries
- [ ] `canon_merge.py` prefers `episode_summary` from `canon_diff.json` if present, falls back to template
- [ ] `gen_script_narration.py` validates output against `contracts/schemas/Script.v1.json` — exit 1 on failure
- [ ] `gen_pipeline_vars.py` writes all 13 pipeline vars (same set as LLM Stage 0 would produce)
- [ ] `voice_cast_narrator.py` is append-only on VoiceCast.json — never overwrites existing character entries
- [ ] `voice_cast_narrator.py` reads `prompts/azure_tts_styles.txt` — same voice catalog as LLM Stage 0
- [ ] `canon_diff_chars.py` idempotency guard: skips if `canon_diff.json` exists with no `__FILL__` tokens
- [ ] `gen_shotlist_scaffold.py` assigns ALL vo_item_ids from Script.json (no orphans) before computing durations
- [ ] `gen_shotlist_scaffold.py` applies min 5s merge / max 45s split rules
- [ ] `gen_shotlist_scaffold.py` computes `timing_lock_hash` = SHA256 of sorted shot_ids
- [ ] `gen_manifest_structure.py` marks exactly 8 creative fields per background as `__FILL__`
- [ ] `gen_vo_manifest.py` accesses VoiceCast locale as `character[locale]` (flat key, not `character["locales"][locale]`)
- [ ] `gen_vo_manifest.py` handles both Script formats: `scene.actions[].line` (type=="dialogue") and `scene.dialogue[]`
- [ ] `validate_scaffold.py` Part A (residual `__FILL__`) always exits 1; Part B (drift) respects `--warn-only`

### After modifying creative fill prompts (prompts/p_4_c.txt, p_5_c.txt, p_6.txt)
- [ ] Prompt reads scaffold JSON from `{{EPISODE_DIR}}` — same file the deterministic script wrote
- [ ] Prompt outputs ONLY the filled JSON (no markdown fences, no explanation text)
- [ ] `p_4_c.txt` fills exactly 7 fields per shot: background_id, camera_framing, camera_movement, sfx_tags, music_mood, sfx_item_ids, music_item_id
- [ ] `p_5_c.txt` fills backgrounds[] visual/search fields + sfx_items[] tags; overwrites shared manifest in-place
- [ ] `p_6.txt` fills new_world_facts, new_unresolved_threads, resolved_threads; outputs complete `canon_diff.json`
- [ ] No `_c` prompt modifies any pre-filled field (timing, IDs, structure) — only fills `__FILL__` markers

### After modifying prompts/azure_tts_styles.txt
- [ ] `supports()` lines present on all en-capable and zh-capable entries
- [ ] No-style voices (`styles(0): []`) are correctly tagged — `parse_azure_tts_styles()` annotates `has_styles=False` automatically
- [ ] MULTITALKER voices appear in a clearly labeled section at the end — `is_multitalker` detected via "ultitalker" in voice name
- [ ] Section headers (e.g. `====== EN-CAPABLE ======`) do not use `[STANDARD]` or `[DRAGON]` — those brackets are reserved for voice tier lines matched by `VOICE_RE`
- [ ] ⚠ Token budget: 275 entries already adds ~17–22K tokens to Stage 0 (haiku). Monitor for quality degradation.

### After modifying media server (code/media/http/)
- [ ] `batch_store.create()` uses `**item_data` spread — does NOT cherry-pick fields from manifest items
- [ ] `server.py` reads API keys from env vars only (`MEDIA_API_KEY`, `PEXELS_API_KEY`, `PIXABAY_API_KEY`) — no keys in `config.json`
- [ ] `server.py POST /batches` handles both list and dict format for `manifest.backgrounds`
- [ ] `scorer.py` backward compat: bare string `item` → `{"ai_prompt": item}` (fallback mode)
- [ ] `scorer.py` multi_dim mode: reads `scoring_hints` dict from item, scores 5 dimensions independently
- [ ] `scorer.py` `score_videos()` adds `duration_sec` field to every video result via `_probe_duration()` (FFprobe) — float or None; field always present on video results (including error results)
- [ ] `downloader.py` populates `item["_thumbnails"]` dict for scorer thumbnail pass-1.5
- [ ] `sequence_ranker.py` reads `.meta.json` sidecars for pairwise discontinuity (hue_hist, mean_luma, clip_embedding)

### After modifying media tab in test_server.py
- [ ] JS regex in `_mediaServerUrl()` uses `/\/+$/` — NOT `/\\/+$/` (Python raw string escaping: `\\/` = two chars in HTML, breaks JS regex parsing)
- [ ] All three media proxy routes (`/api/media_batch`, `/api/media_batches`, `/api/media_batch_status`) inject `MEDIA_API_KEY` from `os.environ`, NOT from request body/query
- [ ] No `<input>`, JS variable, or query param exposes `MEDIA_API_KEY` to the browser
- [ ] `{{MEDIA_SERVER_URL}}` placeholder replaced at HTML serve time in `do_GET` — fallback priority: env var > config.json > empty string
- [ ] `POST /api/media_confirm` writes to `{ep_dir}/assets/media/selections.json` — includes `version` field (1, 2, or 3)
- [ ] `GET /api/episode_file` whitelisted to `ShotList.json` and `selections.json` — returns raw JSON, 403 for non-whitelisted files
- [ ] `_vc_config` loaded once at module level from `code/http/config.json` — server starts even if file missing (empty dict fallback)
- [ ] `top_n` in `/api/media_batch` uses priority chain: `MEDIA_TOP_N` env var → `config.media.top_n` → `5`
- [ ] Per-shot mode: `_mediaShotMap` is null when ShotList unavailable (404) → falls back to single-selection mode (old format)
- [ ] Multi-segment stacking: `_mediaLoadShotMap()` also stores `_mediaShotDur` (shot_id → duration_sec) for fill progress calculation
- [ ] `mediaConfirm()` detects segments format via `hasSegments` check → sends `version: 3` when any per_shot entry has `segments` array
- [ ] `render_video.py` multi-segment: uses FFmpeg `concat` filter (NOT `-stream_loop`) for `background_segments[]` with >1 entry — falls back to single-bg when null/single
- [ ] `_mediaSyncFromRunTab()` propagates `currentSlug`/`currentEpId` from Run tab to Media tab on `switchTab('media')` — only if Media tab has no selection yet
- [ ] Clickable active shot: `_mediaActiveShot[itemId]` tracks which shot row receives new segments — `mediaSelect()` uses this instead of "find first with gap"; shot rows have `media-shot-active` CSS class; clicking a row calls `mediaSetActiveShot()`

### After modifying code/http/vo_retune.py or code/http/retune_vo.py
- [ ] `retune_vo_items()` signature unchanged: `manifest_path, locale, item_ids, scene, patches, per_item_patches, dry_run, backup`
- [ ] Exactly one of `item_ids` / `scene` accepted; both supplied → `ValueError`; neither → `ValueError`
- [ ] `validate_patches()` rejects blocked fields: `azure_voice`, `speaker_id`, `item_id`, `start_sec`, `end_sec`, `duration_sec`
- [ ] Empty `patches` dict is valid (zero-patch re-synthesis) — must NOT be rejected
- [ ] Per-item loop order: snapshot → patch _merged + _draft (atomic) → resynth → commit-or-revert. Never patch-all-then-synthesize-all.
- [ ] Manifest/WAV consistency invariant: on synthesis failure, manifest fields are restored from snapshot (atomic write) before continuing to next item
- [ ] `_write_manifest_atomic()` writes to `{path}.tmp` then `os.replace()` — never writes directly to final path
- [ ] Both manifests patched: `AssetManifest_merged.{locale}.json` (immediate use) AND `AssetManifest_draft.{locale}.json` (survives Stage 10 re-run)
- [ ] Manifest backups use timestamp suffix `{path}.bak.{YYYYMMDD_HHMMSS}` — never overwritten
- [ ] WAV backups use timestamp suffix `{wav}.bak.{YYYYMMDD_HHMMSS}` when `backup=True`
- [ ] `retune_vo_log.jsonl` always appended (never truncated); `status` is `ok/partial/error`
- [ ] Duration drift warning fires when `|after - before| > 0.3s` → `duration_warn: True` in result
- [ ] Scene selector uses manifest index order (NOT sorted by item_id string)
- [ ] Scene fallback: `scene_id` field → regex `sc\d+` from `item_id` → skip with WARNING (not hard fail)
- [ ] `ssml_narration` items: hard error (not silently skipped) — log and return error result
- [ ] CLI `--item / --items / --scene` are mutually exclusive (argparse group)
- [ ] CLI zero-patch (no `--text/--style/--rate/--pitch/--style-degree/--break-ms`): valid — goes straight to re-synthesis
- [ ] `gen_tts_cloud.py --asset-id --force` invoked per item via `subprocess.run()` with `cwd=PIPE_DIR`
- [ ] `/api/retune_vo` returns 400 for `ValueError`/`FileNotFoundError`, 500 for unexpected exceptions
- [ ] `/api/retune_vo` checks `_RETUNE_AVAILABLE` flag; returns 500 with clear message if import failed
- [ ] `retune_vo.py` is in `code/http/` (NOT repo root) — co-located with `vo_retune.py`
- [ ] WebUI `🎙 VO` tab: episode + locale selectors → load vo_items → per-item inline edit → ⟳ button
- [ ] WebUI duration result: shows `before → after` + `⚠️` when `duration_warn` is true

### After modifying contracts/schemas/*.v1.json
- [ ] All LLM-written artifacts still validate against updated schema
- [ ] Fields added as "required" must be generated by the relevant stage
- [ ] Fields removed from schema must not be depended on by downstream Python scripts
- [ ] `AssetManifest.v1.json` backgrounds: 9 optional properties (scoring_hints, search_queries, cinematic_role, motion_level, lighting, include_keywords, exclude_keywords, continuity_hints, source_filters) must remain optional — old manifests without them must still validate

---

## PART 13 — KNOWN ARCHITECTURE DECISIONS (do not accidentally revert)

1. **Prepare → Create Episode → Stage 0 separation**
   The web UI creates the episode directory structure (`meta.json`, `pipeline_vars.sh` stub, `story.txt`) BEFORE Stage 0 runs. Stage 0 ONLY does voice casting — does NOT derive project metadata from story. All metadata comes from meta.json written by `/api/create_episode`.

2. **run.sh old interface is gone**
   `./run.sh story_N.txt` no longer works. Only `./run.sh <ep_dir> [from] [to]`. This was changed in commit `61d570d`.

3. **`VOICE_CAST_FILE` not in stub**
   `pipeline_vars.sh` written by Create Episode does NOT include `VOICE_CAST_FILE`. Stage 0 adds it when it writes the full version. Any code that reads `VOICE_CAST_FILE` before Stage 0 has run will find it undefined.

4. **VoiceCast.json is project-level, not episode-level**
   Lives at `projects/{slug}/VoiceCast.json`, shared across all episodes of a series for voice consistency. Stage 0 appends new characters but never overwrites existing ones.

5. **p_9.txt (old LLM Stage 9) is superseded**
   `gen_render_plan.py` (Stage 9[6/8]) does the same job deterministically. `p_9.txt` still exists on disk but is NOT invoked by run.sh in the current pipeline. The stage formerly called "Stage 10" is now referred to as "Stage 9".

6. **manifest_merge runs before TTS (by design)**
   First run produces approximate duck_intervals (since WAV durations aren't known yet). Re-running steps [2]–[6] after step [4] gives accurate timing. The web UI `/run_locale` endpoint supports re-running individual steps.

7. **azure_style_degree and azure_rate are user-tuned values**
   Stage 5 copies them EXACTLY from VoiceCast.json. No LLM stage may override these per-line. This is the user's tuning controlled by the Voice Cast editor.

8. **generation_seed is random per episode**
   Set once by `/api/create_episode` using `random.randint(100_000_000, 999_999_999)`. Stored in `meta.json` and `pipeline_vars.sh`. Never changes after episode creation. Same seed = same LLM output (deterministic runs).

9. **azure_tts_styles.txt is a single file shared by Stage 0 and the VC editor**
   `prompts/azure_tts_styles.txt` is the only voice catalog file. 275 voices. Pre-embedded by `inline_files_into_prompt` into Stage 0 AND read by `parse_azure_tts_styles()` for `/api/azure_voices`.
   ⚠ Token budget watch: the expanded 275-entry file adds ~17–22K tokens to Stage 0 (haiku). If Stage 0 quality degrades, consider splitting into a styled-only file for Stage 0 and the full file for the VC editor at that point.

10. **WAV item_id names are tied to scene_id values in the Script**
    Stage 4 assigns `vo_item_ids = ["vo-{scene_id}-{NNN}"]` where `{scene_id}` is the slug from
    `Script.json scenes[].scene_id`. If Stage 3 is re-run and scene IDs change (e.g. old slug
    `scene_001_river_night` → new slug `s1`), Stage 5 and all subsequent Stage 9 re-runs produce
    WAVs with the new naming scheme. Old WAV files from the previous run (e.g.
    `vo-scene_001_river_night-001.wav`) remain on disk alongside the new ones (`vo-s1-001.wav`).
    `resolve_assets.py` may find and silently reuse stale WAVs if it happens to probe an old
    `item_id`. The **Purge Cache** feature (`_purge_episode_assets()`) was introduced specifically
    to clean these stale files before a re-run. Without purging, audio in the output mp4 may not
    match the current Script.json even though the SRT subtitles (generated from the current script)
    look correct.

12. **SSML bookmark must be inside `<voice>`, not before it**
    `gen_tts_cloud.py` `build_episode_ssml()` places `<bookmark mark='{id}'/>` as the first child of each `<voice>` block. Azure error 1007 ("RootSpeak should not contain node [bookmark]") fires if the bookmark is placed as a direct child of `<speak>`. Do not "simplify" the SSML structure by hoisting bookmarks outside `<voice>`.

13. **Status Report is per-episode, append-only**
    `projects/{slug}/episodes/{ep_id}/status_report.txt` — each entry is a timestamped block appended by `/api/append_status_report`. Never overwritten. Displayed in the "Status Report" sub-tab of the Run panel. Used to track issues, fixes, and manual steps across pipeline runs.

11. **Purge-before-run is the safe default**
    Any time Stages 1–8 are re-run (which may change scene IDs, VO text, speaker assignments, or
    locale manifests), Stage 9 caches become stale. The 🗑 Purge Cache checkbox defaults to ON so
    that clicking "Run Locale" or "Run Stage 9" always rebuilds from scratch. Users who know the
    caches are still valid (e.g. only re-running render_video after a render-plan tweak) can
    uncheck it to reuse existing WAVs and images. Re-running Stage 9 without purging after
    Stage 3 changed is the most common source of "wrong audio" or "stale image" bugs.

14. **Media API keys are server-side only**
    `MEDIA_API_KEY` is injected by the three proxy routes in `test_server.py` via `os.environ`.
    The old `<input id="media-api-key">` was removed along with `_mediaApiKey()` JS function.
    Do NOT re-add client-side API key fields — the key must never appear in HTML or JS.

15. **file:// URI transport for NFS media**
    Media server writes images/videos to NFS-shared storage (`/data/shared` on server = `/mnt/shared`
    on client). URIs are `file:///mnt/shared/...`. Browsers block `file://` from `http://` pages,
    so the VC editor provides `/api/serve_media_file?url=<encoded>` as a binary proxy.
    JS rewrites `file://` URLs to this proxy before setting `img.src` / `vid.src`.

16. **Media server is a separate deployment**
    Runs on NFS server (`192.168.86.33:8200`), separate from VC editor (`192.168.86.40:8080`).
    They communicate via HTTP. `MEDIA_SERVER_URL` env var on the VC editor configures the
    connection. The media server has its own `requirements.txt` (`code/media/http/requirements.txt`)
    and is self-contained — does NOT depend on `requirements_linux.txt`.

17. **Pipeline hard stops use exit 0, not exit 1**
    The Stage 5 and Stage 8 checkpoints are clean pauses, not errors. `exit 0` ensures the pipeline
    reports success to the caller. The user manually resumes with `./run.sh <ep_dir> 6` or
    `./run.sh <ep_dir> 9`. Do NOT change to `exit 1` — that would cause the SSE stream to report
    a failure to the web UI.

18. **Media server requirements are self-contained**
    `code/media/http/requirements.txt` lists all deps (fastapi, uvicorn, open_clip_torch,
    opencv-python-headless, imagehash, etc.). Install torch CPU first:
    `pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu`
    then `pip install -r code/media/http/requirements.txt`.
    `requirements_linux.txt` does NOT include media server deps — they are separate deployments.

19. **VC editor HTTP connection management (daemon_threads + beforeunload + Connection: close)**
    Three mechanisms work together to prevent "reload hangs after server restart":
    (a) `ReusableServer.daemon_threads = True` — SSE handler threads and video-streaming threads
        are daemon threads, so Ctrl-C kills them immediately. Without this, the old server process
        stays alive as a zombie (non-daemon threads block exit), and the browser tab's existing TCP
        connections are routed to the zombie instead of the new server.
    (b) `Handler.end_headers()` override injects `Connection: close` on every HTTP response. This
        tells the browser not to pool/reuse TCP connections, so after a server restart the browser
        opens fresh connections instead of sending requests on stale dead sockets.
    (c) `window.addEventListener('beforeunload', ...)` closes all EventSource connections, clears
        all polling timers, releases all video connections, and stops audio playback. This ensures
        the browser's per-host connection pool is clean before the reload request fires.
    Do NOT remove any of these three — they address different failure modes and all three are needed.

20. **Video thumbnail connection lifecycle**
    Media tab video thumbnails use a careful connection lifecycle to stay within the browser's
    6-connection-per-host HTTP/1.1 limit:
    (a) Created with `preload='none'` and URL in `dataset.lazySrc` — no connection on creation
    (b) `IntersectionObserver` triggers `_mediaLazyLoad()` when scrolled into view — sets `src`,
        `preload='metadata'`, throttled to max 2 concurrent
    (c) After `loadedmetadata` fires, browser shows first frame; connection idles naturally
    (d) On hover (`mouseenter`): restores `src` from `dataset.lazySrc` if cleared, calls `play()`
    (e) On leave (`mouseleave`): `_mediaStopVid()` removes `src`, calls `load()` to abort download,
        stores URL back in `dataset.lazySrc`. Does NOT re-set `vid.src` (that opens a new connection)
    (f) Before confirm: `_mediaReleaseAllConnections()` aborts ALL actively-downloading videos
    Key invariant: `_mediaStopVid()` must NEVER set `vid.src` after clearing it. The HTML spec says
    setting `.src` always triggers the media element load algorithm, opening an HTTP connection even
    with `preload='none'`.

21. **ssml_narration two-layer SSML architecture**
    `ssml_narration` separates authored SSML into two immutable layers:
    - **Layer 1 (wrapper):** `<speak>` → `<voice>` → `<mstts:express-as>` → `<prosody>` — extracted
      into VoiceCast.json by `ssml_preprocess.py`, rebuilt by `gen_tts_cloud.py build_ssml_narration()`
    - **Layer 2 (inner):** raw text + `<break time="Nms"/>` tags — stored as `ssml_inner.xml`,
      immutable after creation, passed through verbatim during TTS synthesis
    This separation lets the pipeline:
    (a) Use VoiceCast.json for consistent voice params across the pipeline (Stage 4 reads azure_rate)
    (b) Preserve exact authored timing (breaks, pauses) without LLM interference
    (c) Inject `auto_sent_NNN` bookmarks at sentence boundaries for per-item WAV splitting
    Do NOT merge the layers or modify `ssml_inner.xml` after creation — it represents the user's
    authored voice timing exactly as pasted.

23. **Scaffold → LLM Complete → Validate pattern**
    Stages 4 (narration), 5 (narration), and 6 (all formats) use a three-step pattern:
    (a) Python script writes a scaffold JSON with `__FILL__` markers on creative fields only
    (b) `fill_and_run "N_c"` sends scaffold to LLM via `p_N_c.txt` — LLM fills ONLY the markers
    (c) `validate_scaffold.py` post-validates: Part A checks no residual `__FILL__` tokens,
        Part B checks pre-filled fields weren't changed by the LLM
    This pattern guarantees deterministic fields (IDs, durations, structure) cannot be corrupted
    by LLM hallucination while still leveraging creative capability for visual descriptions,
    search queries, and narrative summaries. The `_c` suffix on stage numbers triggers
    `get_stage_model()` and `stage_label()` to strip it via `n_base="${n%%_*}"`.
    Do NOT merge the scaffold and fill steps — the separation is the safety mechanism.

24. **Stages 1, 7, 9 are fully deterministic (no LLM)**
    - Stage 1: `canon_check.py` — reads canon.json, prints structured report
    - Stage 7: `canon_merge.py` — merges canon_diff.json into canon.json
    - p_9.txt: skipped entirely (replaced by `gen_render_plan.py` in Stage 9[6/8])
    These stages have zero creative content — their outputs are fully determined by their inputs.
    Do NOT revert to LLM (`fill_and_run`) for these stages.

25. **Stage 0 narrator format split (gen_pipeline_vars.py + voice_cast_narrator.py)**
    For `continuous_narration`, `illustrated_narration`, `documentary`: Stage 0 runs two Python
    scripts instead of the LLM. `gen_pipeline_vars.py` writes `pipeline_vars.sh` from `meta.json`
    (pure data copy). `voice_cast_narrator.py` parses `azure_tts_styles.txt`, selects best
    narrator voice per locale (prefers `narration-professional` style), and appends to
    `VoiceCast.json`. These formats only need a narrator — no creative casting decisions.
    `episodic` and `monologue` still use the LLM (`fill_and_run 0`) because they require
    multi-character voice casting with personality matching.

26. **Stage 3 narration format determinism (gen_script_narration.py)**
    For `continuous_narration`, `illustrated_narration`, `documentary`: Stage 3 runs
    `gen_script_narration.py` instead of the LLM. The script reads `NarrationText.txt`,
    splits into sentences, groups into scenes (paragraph breaks → section dividers → N/3),
    and produces a valid Script.v1.json with narrator-only cast. All fields are deterministic
    (no creative language generation needed — the text already exists in `NarrationText.txt`).
    `episodic` and `monologue` still use the LLM because they require creative dialogue writing.

27. **`/api/diagnose_pipeline` is fully deterministic**
    The haiku LLM call in `/api/diagnose_pipeline` was replaced with `_run_diagnose()` — a Python
    function that finds the earliest STALE or MISSING stage by comparing input/output timestamps.
    This eliminates an LLM call that added latency and occasionally produced wrong stage numbers.
    Do NOT revert to the haiku call.

28. **SSE subprocess lifetime is decoupled from browser connection**
    Early versions of `/stream`, `/run_locale`, and `/run_stage10` killed the subprocess in a
    `finally` block when the SSE connection dropped (`BrokenPipeError` → `proc.terminate()` → SIGTERM).
    Long-running Stage 9 steps (e.g. polish_locale_vo re-synthesizing 47 items via Azure, ~3 min)
    were silently killed when the browser disconnected or the connection timed out.

    Current architecture uses a persistent log-file per job:
    - `_jobs: dict` — registry of active/completed jobs keyed by `job_key`
    - `_job_log_path(job_key)` — stable tmp path `{TMPDIR}/vc_job_{job_key}.log`
    - `_launch_stream_job(job_key, cmd, env, client)` — starts subprocess in background thread; streams stdout/stderr to log file tagged `O\t{line}` (stdout) / `E\t{line}` (stderr) / `D\t{rc}` (done sentinel)
    - `_launch_fn_job(job_key, target_fn)` — runs a Python function in a background thread; `target_fn` receives a `write_log(tag, line)` callback
    - `_tail_log_to_sse(wfile, log_path)` — tails the log file from the start, streams tagged lines to SSE; stops on `D\t` sentinel or on `BrokenPipeError`; reconnecting client replays the full log from the beginning

    The subprocess/function runs to completion regardless of browser connection state. A reconnecting
    client gets the full log replayed. Do NOT revert to the try/finally proc.terminate() pattern.

29. **`gen_music_clip.py` failure is non-fatal (run.sh `|| true`)**
    Stage 9[1/8] calls `gen_music_clip.py --manifest AssetManifest_draft.shared.json`. Projects
    without a shared manifest (e.g. documentaries that never reached Stage 5) would cause `sys.exit(1)`
    inside gen_music_clip.py, aborting the entire Stage 9 run before any TTS work began.
    `run.sh` now appends `|| true` to this call — the same treatment already applied to
    `music_prepare_loops.py`. Stage 9 continues even if music clip generation fails; the music
    steps simply produce no clips, and render_video falls back to silence for music tracks.
    Do NOT remove the `|| true` — projects without music should not fail Stage 9.

30. **VO Retune: per-item patch-synthesize-commit loop (NOT patch-all-then-synthesize-all)**
    `vo_retune._process_one_item()` is the unit of work — for each item:
    (a) Snapshot current manifest fields in memory
    (b) Patch `_merged` + `_draft` manifests (atomic write: `.tmp` → `os.replace()`)
    (c) Call `gen_tts_cloud.py --asset-id --force`
    (d) Success: leave manifest patched — manifest now matches new WAV ✓
    (e) Failure: restore manifest from snapshot (atomic write) — manifest matches old WAV ✓
    This invariant ("manifest state == WAV on disk for every item at all times") is the
    fundamental correctness guarantee. Violating it by patching all items first and then
    synthesizing leaves failed items with an inconsistent manifest (says "new text/style"
    but WAV is still old), which causes downstream render to play wrong audio.
    Do NOT refactor into two separate passes (patch-all, then synthesize-all).

31. **VO Retune: both manifests patched, for different reasons**
    `retune_vo.py` and `/api/retune_vo` both patch `_merged` AND `_draft`:
    - `_merged` patch → enables immediate re-synthesis without a Stage 10 re-run
      (gen_tts_cloud.py reads from `_merged` at synthesis time)
    - `_draft` patch → survives any future Stage 10 re-run
      (Stage 10[2/8] `manifest_merge.py` unconditionally re-creates `_merged` from `_draft`)
    If only `_merged` is patched: retune works immediately but the next Stage 10 run
    overwrites the patch. If only `_draft` is patched: retune is durable but a Stage 10
    re-run is required to pick it up. Patching both gives both properties.

32. **VO Retune: duration drift is a user responsibility in v1**
    After retune, `start_sec`/`end_sec` in the manifest are NOT updated (they are read-only
    in v1). If WAV duration drifts significantly (> 0.3s), `render_video.py` will place audio
    at the original timestamp — overflow bleeds into the next shot, underrun leaves silence.
    The result object includes `duration_warn: True` when drift exceeds the threshold, giving
    the user a signal but no automatic fix. V2 scope: auto-update `duration_sec` and recompute
    subsequent `start_sec` values after successful retune.

33. **Per-track and per-clip music volume offsets (track_volumes / clip_volumes in MusicPlan)**
    MusicPlan.v1.json now has two optional top-level volume maps:

    **`track_volumes`** `{ stem: dB_offset }` (−18 to 0)
    - Coarse knob: one setting applies to every shot that uses that source stem.
    - Written by the Music tab "vol" input in the Source Music Library card.
    - Applied by apply_music_plan.py AFTER apply_shot_overrides() so that `music_asset_id`
      is already populated on manifest items. Writes `base_db = BASE_MUSIC_DB + offset`.
    - Skips items with `base_db` already set (per-shot priority).

    **`clip_volumes`** `{ key: dB_offset }` (−18 to 0) — dual-key strategy:
    - `item_id` key (e.g. `"music-s01e01_sh01"`) for auto clips → applied directly to manifest.
    - `clip_id` key (e.g. `"djokovic_bg_calm:19.4s-40.3s"`) for user-cut clips → persisted
      immediately; resolved to item_id via shot_overrides[].music_clip_id at apply time.
    - Written by the "Vol" column input in the Generated Clips table.

    **Data flow through the pipeline:**
    ```
    apply_music_plan.py  →  writes base_db onto manifest music_items[]
    gen_render_plan.py   →  reads music_item["base_db"], threads into music_extra["base_db"]
    render_video.py      →  reads shot.get("base_db", BASE_MUSIC_DB), passes to build_duck_expr()
    ```
    Missing any link silently drops base_db — gen_render_plan.py is the critical middle step.

    **Invariants:**
    - `duck_db` remains relative to `base_db` (multiplicative). Existing duck values need no update.
    - Positive boost (+dB) is intentionally excluded (v1): base_db applied before mixing; upstream
      clipping cannot be repaired by the downstream loudnorm (LUFS normalisation ≠ limiter).
    - `_musicTrackVolumes` in the UI is NOT reset when Generate Review reruns; only resets on
      episode change. `_musicCutClips` resets on episode change and is reloaded from disk.
    - MusicPlan.v1.json `additionalProperties: false` — both new fields are explicitly in the schema.
    - AssetManifest music_items `additionalProperties: true` — `base_db` written there needs no schema change.

22. **ssml_narration skips stages 1, 2, 3, 8 — by design**
    These stages are unnecessary because:
    - Stage 1 (canon check): SSML content is self-contained, no world continuity needed
    - Stage 2 (StoryPrompt): no episode direction needed — SSML defines the content
    - Stage 3 (Script): `ssml_preprocess.py` already produces a valid Script.json
    - Stage 8 (locale variants): authored SSML is single-locale by nature
    Stages 4, 5, 6, 7, 9 still run — they handle visual asset generation, which is the core
    value-add of `ssml_narration` (cinematic visuals for pre-authored narration).
